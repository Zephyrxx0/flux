---
phase: 01-deterministic-simulation-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/simulation/contracts/schemaVersion.ts
  - src/simulation/contracts/input.schema.ts
  - src/simulation/contracts/output.schema.ts
  - src/simulation/core/simulateDeterministic.ts
  - src/simulation/adapters/StadiumSim.ts
  - src/simulation/index.ts
  - tests/simulation/contracts.test.ts
  - tests/simulation/determinism.test.ts
  - tests/simulation/throughput-delay.test.ts
  - tests/simulation/invariants.test.ts
  - tests/simulation/properties.test.ts
autonomous: true
requirements:
  - SIM-01
  - SIM-02
must_haves:
  truths:
    - "User can run deterministic simulation inputs against a stable, versioned contract without schema ambiguity."
    - "User can execute fast smoke checks after each task to detect regressions quickly."
    - "Simulation entrypoints are fixed and reusable for downstream phases."
  artifacts:
    - path: "src/simulation/contracts/input.schema.ts"
      provides: "Runtime-validated, schemaVersioned simulation input contract"
    - path: "src/simulation/contracts/output.schema.ts"
      provides: "Runtime-validated, schemaVersioned simulation output contract with matrix/peak/invariants shape"
    - path: "src/simulation/core/simulateDeterministic.ts"
      provides: "Pure simulation function contract and deterministic entrypoint stub"
    - path: "src/simulation/adapters/StadiumSim.ts"
      provides: "Class wrapper contract delegating to pure simulation function"
    - path: "vitest.config.ts"
      provides: "Deterministic test harness configuration with non-watch automation mode"
  key_links:
    - from: "src/simulation/adapters/StadiumSim.ts"
      to: "src/simulation/core/simulateDeterministic.ts"
      via: "StadiumSim.run() delegates to pure core"
      pattern: "simulateDeterministic"
    - from: "src/simulation/core/simulateDeterministic.ts"
      to: "src/simulation/contracts/input.schema.ts"
      via: "Runtime parse/validation before computation"
      pattern: "SimulationInputSchema.parse"
    - from: "src/simulation/core/simulateDeterministic.ts"
      to: "src/simulation/contracts/output.schema.ts"
      via: "Runtime parse/validation before return"
      pattern: "SimulationOutputSchema.parse"
---

<objective>
Establish the deterministic simulation foundation by creating Wave 0 test infrastructure and versioned contracts/interface scaffolding.

Purpose: Lock SIM-01 and SIM-02 contracts and fast feedback infrastructure before algorithm implementation to prevent churn and reduce debugging latency.
Output: Test harness, schema-validated input/output contracts, and exported simulation entrypoints ready for deterministic engine implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-deterministic-simulation-core/01-CONTEXT.md
@.planning/phases/01-deterministic-simulation-core/01-RESEARCH.md
@.planning/phases/01-deterministic-simulation-core/01-VALIDATION.md

Scope lock:
- Implement deterministic simulation core foundations only.
- Do not implement visualization UI, chart rendering, heatmaps, or AI generation flows.
- Honor locked decisions D-01 through D-09 where contracts and interfaces are defined.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave 0 deterministic test harness and smoke feedback setup</name>
  <files>package.json, vitest.config.ts, tests/simulation/contracts.test.ts, tests/simulation/determinism.test.ts, tests/simulation/throughput-delay.test.ts, tests/simulation/invariants.test.ts, tests/simulation/properties.test.ts</files>
  <behavior>
    - Test 1: Contracts test fails when schemaVersion is missing or invalid and passes for valid versioned payload.
    - Test 2: Determinism test asserts identical input returns deeply equal output.
    - Test 3: Throughput-delay test asserts delay blocks throughput before expiry.
  </behavior>
  <action>Install and configure vitest plus fast-check, add npm scripts for test and fast smoke execution, and create Wave 0 test files mapped to SIM-01 and SIM-02. Keep smoke command scoped to the fastest high-signal tests to keep per-task feedback under 30 seconds.</action>
  <verify>
    <automated>npm install && npx vitest run tests/simulation/contracts.test.ts tests/simulation/determinism.test.ts --bail 1</automated>
  </verify>
  <done>Wave 0 test infrastructure is executable, deterministic, and supports sub-30-second smoke feedback for task-level iteration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Define versioned contracts and interface-first simulation entrypoints</name>
  <files>src/simulation/contracts/schemaVersion.ts, src/simulation/contracts/input.schema.ts, src/simulation/contracts/output.schema.ts, src/simulation/core/simulateDeterministic.ts, src/simulation/adapters/StadiumSim.ts, src/simulation/index.ts</files>
  <behavior>
    - Test 1: Input schema parse rejects malformed gate, phase, zone, and arrivals payloads.
    - Test 2: Output schema includes phaseZoneMatrix, peakSummaries, invariants, and schemaVersion.
    - Test 3: StadiumSim wrapper delegates to pure simulateDeterministic contract (D-04).
  </behavior>
  <action>Implement D-04 and D-05 by defining a pure simulateDeterministic contract plus StadiumSim wrapper, both backed by schemaVersioned zod contracts. Implement D-06 output contract fields explicitly (phase-by-zone matrix, peak summaries, invariant flags), including mode structures for D-01/D-02 and detailed-mode two-sub-zone compatibility for D-03. Keep algorithm internals as stubs/placeholders; full deterministic engine logic lands in the next plan.</action>
  <verify>
    <automated>npx vitest run tests/simulation/contracts.test.ts --bail 1</automated>
  </verify>
  <done>Versioned contracts and exported simulation interfaces are stable and validated, with no visualization or AI logic introduced.</done>
</task>

</tasks>

<verification>
- Task-level smoke gate: `npx vitest run tests/simulation/contracts.test.ts tests/simulation/determinism.test.ts --bail 1` stays under 30 seconds.
- Wave gate: run full suite only after plan completion with `npx vitest run`.
- Scope gate: no visualization or AI generation behavior introduced.
</verification>

<success_criteria>
1. SIM-01 and SIM-02 are represented by executable Wave 0 tests and versioned contracts.
2. Simulation input/output schemas enforce schemaVersion and contract validation.
3. Per-task feedback loop uses a fast smoke command with target latency below 30 seconds.
</success_criteria>

<output>
After completion, create .planning/phases/01-deterministic-simulation-core/01-01-SUMMARY.md
</output>
