---
phase: 01-deterministic-simulation-core
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/simulation/core/simulateDeterministic.ts
  - src/simulation/core/enginePhases.ts
  - src/simulation/core/throughput.ts
  - src/simulation/core/invariants.ts
  - src/simulation/core/severity.ts
  - src/simulation/adapters/StadiumSim.ts
  - tests/simulation/determinism.test.ts
  - tests/simulation/throughput-delay.test.ts
  - tests/simulation/invariants.test.ts
  - tests/simulation/properties.test.ts
autonomous: true
requirements:
  - SIM-01
  - SIM-02
must_haves:
  truths:
    - "User can run identical simulation input repeatedly and receive identical outputs (SIM-01)."
    - "User can change gate delay and throughput values and observe occupancy changes by phase and zone (SIM-02)."
    - "Detailed mode remains deterministic and uses exactly two sub-zones per zone for Phase 1."
  artifacts:
    - path: "src/simulation/core/simulateDeterministic.ts"
      provides: "Pure deterministic simulation implementation with stable ordering and invariant output"
    - path: "src/simulation/core/enginePhases.ts"
      provides: "Phase processing logic including overflow carry-over rules"
    - path: "src/simulation/core/throughput.ts"
      provides: "Gate-delay hard blocking and throughput cap calculations"
    - path: "src/simulation/core/severity.ts"
      provides: "Over-capacity severity tier mapping from occupancy ratio"
    - path: "tests/simulation/throughput-delay.test.ts"
      provides: "Executable assertions for delay, throughput, and occupancy effects"
  key_links:
    - from: "src/simulation/core/simulateDeterministic.ts"
      to: "src/simulation/core/enginePhases.ts"
      via: "Core orchestration delegates deterministic phase transitions"
      pattern: "processPhase|enginePhases"
    - from: "src/simulation/core/throughput.ts"
      to: "tests/simulation/throughput-delay.test.ts"
      via: "Behavioral verification of delay blocking and throughput bounds"
      pattern: "delay|throughput"
    - from: "src/simulation/core/invariants.ts"
      to: "tests/simulation/invariants.test.ts"
      via: "Invariant checks for conservation and matrix completeness"
      pattern: "invariant|conservation|matrix"
---

<objective>
Implement deterministic simulation behavior for SIM-01 and SIM-02 using the interfaces and contracts established in 01-01.

Purpose: Complete Phase 1 core behavior while preserving deterministic guarantees and locked decision rules.
Output: Deterministic phase engine with throughput/delay/carry-over logic, severity tiers, and invariant-backed tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-deterministic-simulation-core/01-CONTEXT.md
@.planning/phases/01-deterministic-simulation-core/01-RESEARCH.md
@.planning/phases/01-deterministic-simulation-core/01-VALIDATION.md
@.planning/phases/01-deterministic-simulation-core/01-01-SUMMARY.md

Scope lock:
- Implement deterministic simulation core behavior only.
- Do not add visualization, charting, heatmaps, or AI summary generation.
- Enforce locked decisions D-01 through D-09 in behavior and tests.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Implement deterministic phase engine and constraint processing</name>
  <files>src/simulation/core/simulateDeterministic.ts, src/simulation/core/enginePhases.ts, src/simulation/core/throughput.ts, src/simulation/core/invariants.ts, src/simulation/core/severity.ts, src/simulation/adapters/StadiumSim.ts, tests/simulation/throughput-delay.test.ts, tests/simulation/invariants.test.ts</files>
  <behavior>
    - Test 1: Gate delay hard-blocks throughput before delay expiry (D-08).
    - Test 2: Overflow carries to the next phase in the same zone (D-07).
    - Test 3: Occupancy over 100% is preserved and severity tiers are derived from occupancy ratio (D-09).
  </behavior>
  <action>Implement deterministic integer-state phase processing with stable phase and zone ordering. Enforce D-07 carry-over, D-08 hard delay blocking, and D-09 over-capacity severity mapping. Wire StadiumSim wrapper to the pure core implementation without introducing non-deterministic behavior.</action>
  <verify>
    <automated>npx vitest run tests/simulation/throughput-delay.test.ts tests/simulation/invariants.test.ts --bail 1</automated>
  </verify>
  <done>Constraint handling and invariants are implemented and verified with fast task-level smoke checks under 30 seconds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Finalize deterministic replay behavior and detailed-mode assertions</name>
  <files>src/simulation/core/simulateDeterministic.ts, tests/simulation/determinism.test.ts, tests/simulation/properties.test.ts</files>
  <behavior>
    - Test 1: Same input replayed multiple times returns deeply equal output payloads (SIM-01).
    - Test 2: Detailed mode remains deterministic with exactly two sub-zones per zone (D-01, D-02, D-03).
    - Test 3: Property checks preserve deterministic and conservation invariants across generated inputs.
  </behavior>
  <action>Complete deterministic replay implementation and lock detailed-mode behavior to manual toggle with exactly two sub-zones per zone. Strengthen deterministic and property-based tests so SIM-01 and SIM-02 are both covered without expanding Phase 1 scope.</action>
  <verify>
    <automated>npx vitest run tests/simulation/determinism.test.ts tests/simulation/properties.test.ts --bail 1</automated>
  </verify>
  <done>Deterministic replay guarantees are test-proven and detailed-mode constraints are enforced exactly as locked decisions require.</done>
</task>

</tasks>

<verification>
- Task-level smoke gate: each task verify command targets a focused subset and should complete under 30 seconds.
- Wave gate: run `npx vitest run` after completing this plan.
- Requirement gate: SIM-01 and SIM-02 tests are green before phase verification.
- Scope gate: no UI or AI features introduced.
</verification>

<success_criteria>
1. SIM-01 deterministic replay guarantee is automated and consistently green.
2. SIM-02 gate-delay and throughput rules materially affect occupancy by phase/zone with carry-over conservation.
3. Full suite passes at wave/phase gate while task-level checks remain fast and executable.
</success_criteria>

<output>
After completion, create .planning/phases/01-deterministic-simulation-core/01-02-SUMMARY.md
</output>
