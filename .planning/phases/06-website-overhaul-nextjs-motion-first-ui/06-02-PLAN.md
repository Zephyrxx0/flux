---
phase: 06-website-overhaul-nextjs-motion-first-ui
plan: 02
type: execute
wave: 1
depends_on: [06-01-PLAN.md]
files_modified:
  - src/components/layout/MagneticDock.tsx
  - src/components/layout/AppLayout.tsx
  - src/visualization/components/VisualizationWorkspace.tsx
  - src/index.css
  - tests/ui/layout.test.ts
autonomous: true
requirements: [UI-06-02, UI-06-03]
must_haves:
  truths:
    - "Navigation uses magnetic dock interaction and supports smooth section jumps."
    - "Dock integration does not break the main scenario -> visualization -> reporting workflow."
    - "Dock interactions leverage animejs for motion behavior."
  artifacts:
    - path: "src/components/layout/MagneticDock.tsx"
      provides: "dock interaction and magnetic motion behavior"
      exports: ["MagneticDock"]
  key_links:
    - from: "src/components/layout/MagneticDock.tsx"
      to: "src/visualization/components/VisualizationWorkspace.tsx"
      via: "dock items navigate to section anchors"
      pattern: "compare|report|deploy"
---

<objective>
Implement magnetic dock navigation and section IA anchors for the redesigned shell.

Purpose: Enable fast, tactile navigation between major workflow surfaces while keeping interactions smooth and discoverable.
Output: animejs-powered dock, stable anchor map, and integrated shell behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-website-overhaul-nextjs-motion-first-ui/06-CONTEXT.md
@src/components/layout/AppLayout.tsx
@src/visualization/components/VisualizationWorkspace.tsx
@.agents/skills/frontend-design/SKILL.md
@.agents/skills/ui-ux-pro-max/SKILL.md
@.agents/skills/vercel-react-best-practices/SKILL.md
</context>

<skills_to_apply>
- `frontend-design`: keep dock behavior bold but intentional, with motion that supports navigation clarity.
- `ui-ux-pro-max`: enforce touch target, spacing, reduced-motion readiness, and responsive interaction quality.
- `vercel-react-best-practices`: avoid avoidable re-render and event-listener inefficiencies in dock interaction logic.
</skills_to_apply>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build dock interaction component</name>
  <files>src/components/layout/MagneticDock.tsx, src/index.css</files>
  <read_first>src/components/layout/AppLayout.tsx</read_first>
  <action>Implement a dock component inspired by magnetic navigation patterns, using animejs for proximity pull, scale, and reset transitions.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Dock remains visible and accessible on desktop/mobile widths.
    - Pointer movement produces controlled magnetic response with smooth reset.
    - Dock interaction implementation follows React performance guidance from vercel-react-best-practices.
  </acceptance_criteria>
  <done>Dock behavior feels polished and deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire section navigation map</name>
  <files>src/components/layout/AppLayout.tsx, src/visualization/components/VisualizationWorkspace.tsx</files>
  <read_first>src/components/layout/MagneticDock.tsx</read_first>
  <action>Add and maintain section anchors for overview/simulate/compare/report/deploy surfaces and ensure dock actions scroll to target sections reliably.</action>
  <verify>npm run test -- tests/ui/layout.test.ts</verify>
  <acceptance_criteria>
    - Dock click actions scroll to matching section IDs.
    - Existing workflow content still renders and remains reachable.
    - Section navigation remains accessible and responsive under ui-ux-pro-max interaction rules.
  </acceptance_criteria>
  <done>Navigation model is complete and integrated.</done>
</task>

</tasks>
