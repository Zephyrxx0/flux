---
phase: 14-server-runtime-match-polling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/match.ts
  - src/stores/slices/matchSlice.ts
  - src/lib/api/worldcup26.ts
  - tests/lib/api/worldcup26.test.ts
autonomous: true
requirements: [LIVE-01]
user_setup: []
must_haves:
  truths:
    - "MatchState type accommodates null minute values (pre-match / half-time / full-time have no minute)"
    - "worldcup26.ir raw API response is validated by a Zod schema before any processing"
    - "A mapping function normalizes worldcup26.ir fields to MatchState: score, phase, minute, homeTeam, awayTeam"
    - "A live match heuristic selects the first match that has started but not finished"
    - "An upcoming match fallback finds the next scheduled match by local_date when no live match exists"
    - "parseTimeElapsed handles all known time_elapsed formats: notstarted, numeric, HT, FT, stoppage time"
    - "matchSlice.match type is properly typed as MatchState | null instead of inline partial type"
  artifacts:
    - path: "src/types/match.ts"
      provides: "MatchStateSchema with nullable minute + MatchState type"
      min_lines: 11
    - path: "src/stores/slices/matchSlice.ts"
      provides: "MatchSlice with typed MatchState import"
      min_lines: 15
    - path: "src/lib/api/worldcup26.ts"
      provides: "WorldCup26GameSchema, WorldCup26GamesSchema, MatchPhase type, LiveMatch type, parseTimeElapsed, mapGameToMatchState, findLiveMatch, findNextUpcoming"
      min_lines: 120
    - path: "tests/lib/api/worldcup26.test.ts"
      provides: "Unit tests for all mapping functions"
      min_lines: 80
  key_links:
    - from: "src/lib/api/worldcup26.ts"
      to: "src/types/match.ts"
      via: "imports MatchStateSchema"
      pattern: "import.*MatchStateSchema"
    - from: "src/stores/slices/matchSlice.ts"
      to: "src/types/match.ts"
      via: "imports MatchState type"
      pattern: "import.*MatchState"
---

<objective>
Create the data mapping layer and type updates for the worldcup26.ir API integration.

Purpose: Establish typed contracts between the external worldcup26.ir API and the internal MatchState representation, so all downstream consumers (API proxy, polling hook, MatchBanner) work against validated, normalized types.

Output:
- Updated `src/types/match.ts` (minute nullable per D-14-03/D-14-04)
- Updated `src/stores/slices/matchSlice.ts` (typed MatchState import per PATTERNS.md)
- New `src/lib/api/worldcup26.ts` (Zod schemas for raw API shape, mapping functions, live match heuristic)
- New `tests/lib/api/worldcup26.test.ts` (unit tests for all mapping edge cases)
</objective>

<execution_context>
@/home/zeph/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/zeph/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-server-runtime-match-polling/14-CONTEXT.md
@.planning/phases/14-server-runtime-match-polling/14-PATTERNS.md
@.planning/phases/14-server-runtime-match-polling/14-RESEARCH.md
@src/types/match.ts
@src/stores/slices/matchSlice.ts
@src/simulation/contracts/input.schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MatchStateSchema with nullable minute</name>
  <files>src/types/match.ts</files>
  <read_first>src/types/match.ts</read_first>
  <action>
    Per D-14-03/D-14-04, worldcup26.ir returns `time_elapsed` which can be `"notstarted"`, `"HT"`, `"FT"`, or `"N'"`/`"N+M'"`. When `time_elapsed` is `"notstarted"`, `"HT"`, or `"FT"`, there is no active minute — minute must be `null`.

    Change `minute: z.number().int().min(0).max(120)` to `minute: z.number().int().min(0).max(120).nullable()`.

    Keep all other fields: `score` (z.string()), `phase` (z.enum with 5 values), `homeTeam` (z.string()), `awayTeam` (z.string()). Keep the zod@4 API: `.parse()` returns typed result.

    The `MatchState` type (built via `z.infer`) will automatically reflect `minute: number | null`.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/types/match.ts 2>&1 | grep -c "error" | grep -q "^0$" && echo "TypeScript OK"</automated>
  </verify>
  <done>
    MatchStateSchema.minute accepts null; MatchState type reflects minute: number | null; TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update matchSlice.ts with typed MatchState import</name>
  <files>src/stores/slices/matchSlice.ts</files>
  <read_first>src/stores/slices/matchSlice.ts</read_first>
  <action>
    Per PATTERNS.md, the matchSlice currently has an inline type for its `match` field:
    ```
    match: { score: string | null; phase: string | null; minute: number | null; homeTeam: string | null; awayTeam: string | null } | null
    ```

    Replace the entire `MatchSlice` interface with a canonical import-based version:

    - Import `{ type MatchState }` from `@/types/match`
    - Change `MatchSlice.match` to `MatchState | null`
    - Change `setMatch` parameter to `(match: MatchState | null) => void`

    Keep the file's other structure identical (createMatchSlice function, StateCreator pattern).
  </action>
  <verify>
    <automated>npx vitest run tests/ui/store.test.ts --bail 1 2>&1 | tail -5 | grep -q "passed" && echo "Store tests pass"</automated>
  </verify>
  <done>
    matchSlice.ts imports MatchState from @/types/match; match field typed as MatchState | null; setMatch accepts MatchState | null; store tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create worldcup26.ts API utility + unit tests</name>
  <files>src/lib/api/worldcup26.ts, tests/lib/api/worldcup26.test.ts</files>
  <read_first>
    @src/lib/api/worldcup26.ts (create)
    @src/simulation/contracts/input.schema.ts (Zod schema pattern reference)
    @src/types/match.ts (MatchStateSchema guide)
  </read_first>
  <behavior>
    - Test 1: parseTimeElapsed("notstarted") returns phase="pre-match", minute=null
    - Test 2: parseTimeElapsed("45'") returns phase="first-half", minute=45
    - Test 3: parseTimeElapsed("90+3'") returns phase="second-half", minute=90
    - Test 4: parseTimeElapsed("HT") returns phase="half-time", minute=45
    - Test 5: parseTimeElapsed("FT") returns phase="full-time", minute=90
    - Test 6: mapGameToMatchState with home_score="3", away_score="1", home_team_name_en="Brazil", away_team_name_en="Argentina", time_elapsed="67'", finished="FALSE" returns score="3 - 1", homeTeam="Brazil", awayTeam="Argentina", phase="second-half", minute=67
    - Test 7: mapGameToMatchState with time_elapsed="notstarted" returns phase="pre-match", minute=null
    - Test 8: findLiveMatch returns first game where time_elapsed != "notstarted" && finished === "FALSE"
    - Test 9: findLiveMatch returns null when all games are finished or not started
    - Test 10: findNextUpcoming returns the game with the earliest local_date among notstarted games
    - Test 11: WorldCup26GameSchema rejects a game with finished="TRUE" (not a boolean violation, but schema must accept it via z.enum)
    - Test 12: WorldCup26GameSchema rejects a game with finished="true" (lowercase, not in enum)
  </behavior>
  <action>
    Create `src/lib/api/worldcup26.ts` with the following exports using zod@4 API (typescript@5.9, zod@4.3.6; use `.parse()` returning typed value, `.safeParse()` returning `{success, data?, error?}`):

    1. `WorldCup26GameSchema` — Zod object covering ALL documented worldcup26.ir fields: _id, id, home_team_id, away_team_id, home_score (z.string()), away_score (z.string()), home_scorers (z.string().nullable()), away_scorers (z.string().nullable()), group, matchday, local_date (z.string()), persian_date, stadium_id, finished (z.enum(["TRUE", "FALSE"]) CRITICAL — string not boolean), time_elapsed (z.str), type, home_team_name_en, home_team_name_fa, away_team_name_en, away_team_name_fa
    2. `WorldCup26GamesSchema` — z.object({ games: z.array(WorldCup26GameSchema) })
    3. `WorldCup26Game` type via z.infer
    4. `MatchPhase` type — union of "pre-match" | "first-half" | "half-time" | "second-half" | "full-time"
    5. `LiveMatch` interface — { score: string; phase: MatchPhase; minute: number | null; homeTeam: string; awayTeam: string }
    6. `parseTimeElapsed(timeElapsed: string)` — returns { phase: MatchPhase; minute: number | null }
       - "notstarted" → { phase: "pre-match", minute: null }
       - "HT" → { phase: "half-time", minute: 45 }
       - "FT" → { phase: "full-time", minute: 90 }
       - Numeric via regex /^(\d+)/: minute ≤ 45 → first-half, else second-half
       - Default → { phase: "pre-match", minute: null }
    7. `mapGameToMatchState(game: WorldCup26Game)` — returns LiveMatch with score string combining home_score + " - " + away_score, phase/minute from parseTimeElapsed, homeTeam/awayTeam from name_en fields
    8. `findLiveMatch(games: WorldCup26Game[])` — returns first game where time_elapsed !== "notstarted" && finished === "FALSE", or null
    9. `findNextUpcoming(games: WorldCup26Game[])` — filters games where time_elapsed === "notstarted", sorts by local_date ascending, returns first (or null if none)

    Create `tests/lib/api/worldcup26.test.ts` with vitest describe/it pattern. Import functions from @/lib/api/worldcup26. Cover all 12 behaviors from the &lt;behavior&gt; block. Use realistic worldcup26.ir mock data: "Brazil", "Argentina", "Germany", "France" for team names, and example scores like "3"/"1", "2"/"2", "0"/"0".

    Run `npx vitest run tests/lib/api/worldcup26.test.ts -x` to verify all pass.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/api/worldcup26.test.ts -x</automated>
  </verify>
  <done>
    worldcup26.ts exports all 9 symbols; all 12 test cases pass; parseTimeElapsed handles all known time_elapsed formats; findLiveMatch and findNextUpcoming return correct matches.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

### New Symbols
| Symbol | File | Description |
|--------|------|-------------|
| `WorldCup26GameSchema` | `src/lib/api/worldcup26.ts` | Zod schema validating worldcup26.ir raw game object |
| `WorldCup26GamesSchema` | `src/lib/api/worldcup26.ts` | Zod schema wrapping games array |
| `WorldCup26Game` | `src/lib/api/worldcup26.ts` | Inferred type from WorldCup26GameSchema |
| `MatchPhase` | `src/lib/api/worldcup26.ts` | Union type: pre-match / first-half / half-time / second-half / full-time |
| `LiveMatch` | `src/lib/api/worldcup26.ts` | Normalized match interface |
| `parseTimeElapsed` | `src/lib/api/worldcup26.ts` | Pure function: time_elapsed string → { phase, minute } |
| `mapGameToMatchState` | `src/lib/api/worldcup26.ts` | Pure function: WorldCup26Game → LiveMatch |
| `findLiveMatch` | `src/lib/api/worldcup26.ts` | Pure function: games[] → live match or null |
| `findNextUpcoming` | `src/lib/api/worldcup26.ts` | Pure function: games[] → next scheduled or null |
| `MatchStateSchema.minute` nullable | `src/types/match.ts` | Updated field: z.number().nullable() |

### Modified Symbols
| Symbol | File | Change |
|--------|------|--------|
| `MatchSlice.match` | `src/stores/slices/matchSlice.ts` | Type changed from inline partial to `MatchState \| null` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worldcup26.ir API → lib/api/worldcup26.ts | Untrusted external API response enters at the Zod schema boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | worldcup26.ts Zod schema | mitigate | WorldCup26GameSchema with z.enum(["TRUE", "FALSE"]) prevents string-bool confusion (RESEARCH.md Pitfall 5). safeParse() catches structural mismatches gracefully. |
</threat_model>

<verification>
- `npx vitest run tests/lib/api/worldcup26.test.ts -x` passes (all 12 cases)
- `npx tsc --noEmit src/types/match.ts` produces no errors
- `npx vitest run tests/ui/store.test.ts --bail 1` passes (matchSlice change doesn't break existing stores)
- All exported symbols are visible via TypeScript autocompletion
</verification>

<success_criteria>
1. MatchStateSchema.minute accepts null
2. matchSlice.match typed as MatchState | null (not inline type)
3. WorldCup26GameSchema validates all 18 worldcup26.ir fields including finished as z.enum
4. parseTimeElapsed handles: notstarted, numeric, stoppage time, HT, FT, unknown
5. mapGameToMatchState produces correct score formatting and phase derivation
6. findLiveMatch selects first active match; returns null when none active
7. findNextUpcoming finds earliest future match
8. All tests pass
</success_criteria>

<output>
`.planning/phases/14-server-runtime-match-polling/14-01-SUMMARY.md`
</output>
