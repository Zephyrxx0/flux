---
phase: 03-decision-grade-risk-visualization
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/components/layout/AppLayout.tsx
  - src/visualization/components/StadiumHeatmap.tsx
  - src/visualization/components/RiskLegend.tsx
  - src/visualization/contracts/stadiumPolygons.ts
  - src/visualization/components/VisualizationWorkspace.tsx
  - src/visualization/components/WorkspaceKinoProgress.tsx
  - tests/ui/layout.test.ts
  - tests/ui/visualization.heatmap.test.ts
  - tests/ui/visualization.motion.test.ts
autonomous: true
requirements: [VIZ-02, VIZ-03]
must_haves:
  truths:
    - "User can view risk as a stadium-silhouette heatmap using polygon zone regions (D-03, D-04)."
    - "Heatmap, chart, and legend all use the same green/amber/red threshold semantics (D-05, D-06)."
    - "All visualization state changes animate quickly while preserving state clarity, including reduced-motion handling (D-07, D-08)."
    - "Workspace-level UX polish uses react-kino progress/reveal utilities and animejs transitions without breaking deterministic risk rendering."
  artifacts:
    - path: "src/visualization/contracts/stadiumPolygons.ts"
      provides: "hand-drawn polygon coordinate map keyed by zoneId"
      exports: ["STADIUM_ZONE_POLYGONS"]
    - path: "src/visualization/components/StadiumHeatmap.tsx"
      provides: "SVG silhouette heatmap with polygon fills from shared risk mapping"
      exports: ["StadiumHeatmap"]
    - path: "src/visualization/components/VisualizationWorkspace.tsx"
      provides: "composed workspace (chart + heatmap + shared legend) bound to latest simulation output"
      exports: ["VisualizationWorkspace"]
    - path: "src/visualization/components/WorkspaceKinoProgress.tsx"
      provides: "react-kino progress/reveal shell for visualization workspace orientation"
      exports: ["WorkspaceKinoProgress"]
  key_links:
    - from: "src/components/layout/AppLayout.tsx"
      to: "src/visualization/components/VisualizationWorkspace.tsx"
      via: "main workspace mount"
      pattern: "<VisualizationWorkspace"
    - from: "src/visualization/components/StadiumHeatmap.tsx"
      to: "src/visualization/contracts/riskEncoding.ts"
      via: "shared thresholds and legend bands"
      pattern: "riskBandFromRatio|RISK_THRESHOLDS"
    - from: "src/visualization/components/VisualizationWorkspace.tsx"
      to: "src/hooks/useScenarioStore.ts"
      via: "reads latestSimulationOutput and computes model"
      pattern: "latestSimulationOutput"
    - from: "src/visualization/components/StadiumHeatmap.tsx"
      to: "src/visualization/contracts/motionPolicy.ts"
      via: "animejs-driven heatmap transition policy"
      pattern: "VISUALIZATION_TRANSITION_MS|VISUALIZATION_EASE"
---

<objective>
Deliver the stadium-style heatmap and fully wire the Phase 3 visualization workspace into the application shell with shared legend and transition clarity.

Purpose: Complete decision-grade risk interpretation by combining chart + map views under one consistent visual language.
Output: Polygon heatmap, shared legend, workspace composition with react-kino polish, animejs motion coherence, app integration, and VIZ-02/VIZ-03 validation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-decision-grade-risk-visualization/03-CONTEXT.md
@.planning/phases/03-decision-grade-risk-visualization/03-RESEARCH.md
@.planning/phases/03-decision-grade-risk-visualization/03-02-PLAN.md
@src/components/layout/AppLayout.tsx
@src/visualization/contracts/riskEncoding.ts
@src/visualization/selectors/buildVisualizationModel.ts

<interfaces>
From src/components/layout/AppLayout.tsx (existing integration point):
```tsx
<SidebarInset data-testid="app-main-content">
  <main className="p-6">
    <h2 className="text-xl font-medium">Visualization Workspace</h2>
    <p className="mt-2 text-sm text-slate-600">...</p>
  </main>
</SidebarInset>
```

From src/hooks/useScenarioStore.ts (from 03-01):
```ts
latestSimulationOutput: SimulationOutput | null
setLatestSimulationOutput: (output: SimulationOutput | null) => void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement stadium silhouette polygon heatmap and shared risk legend</name>
  <files>src/visualization/contracts/stadiumPolygons.ts, src/visualization/components/StadiumHeatmap.tsx, src/visualization/components/RiskLegend.tsx, tests/ui/visualization.heatmap.test.ts</files>
  <behavior>
    - Test 1: Heatmap renders polygon regions keyed by zoneId (D-03, D-04).
    - Test 2: Polygon fill colors match shared threshold mapping and legend semantics (D-05, D-06).
    - Test 3: Missing/extra zones degrade gracefully (no crashes; clear fallback fill/state).
  </behavior>
  <action>Create hand-authored polygon coordinates for known zones in `stadiumPolygons.ts` (per D-04) and render SVG silhouette heatmap in `StadiumHeatmap`. Do not implement tile grid fallback (per D-03). Add `RiskLegend` driven only by shared risk constants from `riskEncoding.ts` so chart + heatmap legend text and thresholds cannot diverge (per D-06). Add heatmap tests that verify polygon rendering and threshold-consistent fill/legend behavior.</action>
  <verify>
    <automated>npm run test -- tests/ui/visualization.heatmap.test.ts</automated>
  </verify>
  <done>Heatmap and legend are implemented with stadium-like polygons and strict threshold consistency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose visualization workspace and integrate into App layout</name>
  <files>src/visualization/components/VisualizationWorkspace.tsx, src/visualization/components/WorkspaceKinoProgress.tsx, src/components/layout/AppLayout.tsx, tests/ui/layout.test.ts</files>
  <behavior>
    - Test 1: App main content mounts visualization workspace (chart + heatmap + legend).
    - Test 2: Workspace reflects latest simulation output from store.
    - Test 3: Empty-state messaging appears when no run output exists.
    - Test 4: react-kino workspace progress/reveal shell does not alter data correctness or accessibility.
  </behavior>
  <action>Create `VisualizationWorkspace` that reads `latestSimulationOutput`, computes the normalized visualization model, and composes `RiskLineChart`, `StadiumHeatmap`, and `RiskLegend`. Add `WorkspaceKinoProgress` using react-kino `Progress`/`Reveal` primitives for non-invasive orientation and progressive disclosure. Replace placeholder Phase 3 copy in `AppLayout` with workspace mount. Use shadcn MCP-assisted composition for workspace controls/cards where applicable. Keep layout responsive and preserve existing sidebar behavior from Phase 2.</action>
  <verify>
    <automated>npm run test -- tests/ui/layout.test.ts</automated>
  </verify>
  <done>Phase 3 visuals are fully wired into the shell and visible in the main workspace with deterministic data flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate cross-view transition clarity and reduced-motion policy</name>
  <files>src/visualization/components/RiskLineChart.tsx, src/visualization/components/StadiumHeatmap.tsx, tests/ui/visualization.motion.test.ts</files>
  <behavior>
    - Test 1: Run updates and interaction changes animate in both chart and heatmap (D-07).
    - Test 2: Duration stays inside 150-250ms policy via shared constant (D-08).
    - Test 3: Reduced-motion preference disables/minimizes interpolation while preserving immediate state clarity.
  </behavior>
  <action>Apply shared transition policy to heatmap fills/highlights using animejs and confirm chart transitions use the same policy contract. Add reduced-motion branch using media query check to shorten/disable interpolation while still applying final visual states deterministically. Validate that react-kino-driven reveals/progress also respect reduced-motion settings. Add motion test coverage across both views to ensure VIZ-03 is met at workspace level.</action>
  <verify>
    <automated>npm run test -- tests/ui/visualization.motion.test.ts</automated>
  </verify>
  <done>VIZ-03 is validated across chart and heatmap with consistent, readable motion behavior.</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/ui/visualization.heatmap.test.ts tests/ui/visualization.motion.test.ts tests/ui/layout.test.ts`
- `npm run build`
</verification>

<success_criteria>
- VIZ-02 is complete with stadium polygon heatmap and shared threshold-consistent legend.
- VIZ-03 is complete across both views with fast transitions and reduced-motion handling.
- App layout hosts production visualization workspace instead of placeholder content.
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-grade-risk-visualization/03-03-SUMMARY.md`
</output>
