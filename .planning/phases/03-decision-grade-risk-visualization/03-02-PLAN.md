---
phase: 03-decision-grade-risk-visualization
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/visualization/components/RiskLineChart.tsx
  - src/visualization/components/TopZonesToggle.tsx
  - src/visualization/components/ChartRevealShell.tsx
  - src/visualization/selectors/buildVisualizationModel.ts
  - tests/ui/visualization.chart.test.ts
autonomous: true
requirements: [VIZ-01, VIZ-03]
must_haves:
  truths:
    - "User can view a multi-line D3 chart comparing zones in a single view (D-01)."
    - "Chart defaults to top-5 highest-risk zones and can toggle to all zones (D-02)."
    - "Chart updates animate with fast, readable transitions for reruns/interactions (D-07, D-08)."
    - "Chart container reveal and micro-motion patterns use the agreed UX toolchain (react-kino + animejs) while preserving readability."
  artifacts:
    - path: "src/visualization/components/RiskLineChart.tsx"
      provides: "D3 multi-line SVG chart with keyed zone series"
      exports: ["RiskLineChart"]
    - path: "src/visualization/components/TopZonesToggle.tsx"
      provides: "top-5 vs all toggle control for chart scope"
      exports: ["TopZonesToggle"]
    - path: "src/visualization/components/ChartRevealShell.tsx"
      provides: "react-kino Scene/Reveal wrapper for chart entrance and progressive disclosure"
      exports: ["ChartRevealShell"]
    - path: "tests/ui/visualization.chart.test.ts"
      provides: "automated checks for VIZ-01 and chart-side VIZ-03 behavior"
      contains: "top-5 default and show-all assertions"
  key_links:
    - from: "src/visualization/components/RiskLineChart.tsx"
      to: "src/visualization/selectors/buildVisualizationModel.ts"
      via: "normalized series input"
      pattern: "buildVisualizationModel"
    - from: "src/visualization/components/RiskLineChart.tsx"
      to: "src/visualization/contracts/riskEncoding.ts"
      via: "shared color/threshold semantics"
      pattern: "riskBandFromRatio|RISK_THRESHOLDS"
    - from: "src/visualization/components/TopZonesToggle.tsx"
      to: ".vscode/mcp.json"
      via: "shadcn MCP-assisted component composition workflow"
      pattern: "shadcn"
---

<objective>
Implement the D3 multi-line chart experience with top-5 default scope and user-controlled show-all behavior.

Purpose: Deliver the primary comparative risk view for phase-by-phase zone interpretation.
Output: Production chart component + shadcn MCP-assisted toggle control + UX reveal shell + focused chart tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-decision-grade-risk-visualization/03-CONTEXT.md
@.planning/phases/03-decision-grade-risk-visualization/03-RESEARCH.md
@.planning/phases/03-decision-grade-risk-visualization/03-01-PLAN.md
@src/simulation/contracts/output.schema.ts
@src/visualization/contracts/riskEncoding.ts
@src/visualization/selectors/buildVisualizationModel.ts

<interfaces>
From src/visualization/selectors/buildVisualizationModel.ts (from 03-01):
```ts
export type VisualizationModel = {
  phases: string[]
  zoneSeries: Array<{ zoneId: string; points: Array<{ phaseId: string; occupancyRatio: number }> }>
  latestZoneRatios: Record<string, number>
  topZoneIds: string[]
}
export function buildVisualizationModel(output: SimulationOutput): VisualizationModel
```

From src/visualization/contracts/riskEncoding.ts (from 03-01):
```ts
export const RISK_THRESHOLDS: readonly [0.8, 0.95]
export const VISUALIZATION_TRANSITION_MS: number // 150-250
export function riskBandFromRatio(ratio: number): "green" | "amber" | "red"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build chart controls and keyed D3 multi-line renderer</name>
  <files>src/visualization/components/TopZonesToggle.tsx, src/visualization/components/RiskLineChart.tsx, src/visualization/components/ChartRevealShell.tsx</files>
  <behavior>
    - Test 1: Chart renders one line per visible zone with shared phase x-axis.
    - Test 2: Default visible set is top 5 zones; toggle reveals all zones.
    - Test 3: Series identity remains stable across updates (keyed by zoneId).
    - Test 4: Chart reveal shell mounts without hydration or motion-policy regressions.
  </behavior>
  <action>Use shadcn MCP workflow to scaffold/refine `TopZonesToggle` with project-consistent UI primitives. Implement `RiskLineChart` using D3 scales and line generator with one SVG chart containing all visible zone lines (per D-01). Default to top-5 using selector-provided deterministic ranking with a user toggle to all zones (per D-02). Ensure color encodings come from shared risk module (per D-06). Use keyed joins by `zoneId` to avoid flicker during data/scope updates. Add `ChartRevealShell` using `react-kino` (`Kino` + `Scene`/`Reveal`) to provide controlled progressive entrance without changing data semantics.</action>
  <verify>
    <automated>npm run test -- tests/ui/visualization.chart.test.ts -t "top 5"</automated>
  </verify>
  <done>Chart component renders comparative multi-zone risk lines with deterministic top-5 default and functioning show-all control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce fast, readable chart transitions and verify VIZ-01 behavior</name>
  <files>src/visualization/components/RiskLineChart.tsx, tests/ui/visualization.chart.test.ts</files>
  <behavior>
    - Test 1: Run updates and toggle changes trigger transitions (D-07).
    - Test 2: Transition duration uses shared constant constrained to 150-250ms (D-08).
    - Test 3: Chart remains readable during updates (stable axes/domain, no line identity swap).
  </behavior>
  <action>Implement enter/update transitions for line path and emphasis opacity using `animejs` timeline primitives bound to shared motion policy (`VISUALIZATION_TRANSITION_MS`, ease-out) (per D-07, D-08). Keep y-domain stable by policy to preserve visual interpretability during updates (per research guidance). Add/expand chart tests for line count, legend/color semantics, transition policy usage, and reduced-motion fallback behavior.</action>
  <verify>
    <automated>npm run test -- tests/ui/visualization.chart.test.ts</automated>
  </verify>
  <done>VIZ-01 is test-covered with chart rendering, top-5/show-all interaction, and fast transition policy behavior.</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/ui/visualization.chart.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Multi-line D3 chart is implemented with single-view zone comparison and deterministic scope toggle.
- Transition behavior satisfies D-07 and D-08 without readability regressions.
- Chart behavior is validated by dedicated UI tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-grade-risk-visualization/03-02-SUMMARY.md`
</output>
