---
phase: 03-decision-grade-risk-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .vscode/mcp.json
  - src/hooks/useScenarioStore.ts
  - src/components/config/ScenarioForm.tsx
  - src/visualization/contracts/riskEncoding.ts
  - src/visualization/contracts/motionPolicy.ts
  - src/visualization/selectors/buildVisualizationModel.ts
  - tests/ui/fixtures/simulationOutput.ts
  - tests/ui/store.test.ts
  - tests/ui/run.test.ts
autonomous: true
requirements: [VIZ-02, VIZ-03]
must_haves:
  truths:
    - "Both chart and heatmap derive risk bands from the same locked thresholds (D-05, D-06)."
    - "Latest simulation output is available in shared UI state immediately after Run."
    - "Animation policy values are centralized and constrained to fast transitions (D-08)."
  artifacts:
    - path: "src/visualization/contracts/riskEncoding.ts"
      provides: "single threshold/color/legend policy used by all visualizations"
      exports: ["RISK_THRESHOLDS", "riskBandFromRatio", "RISK_BAND_ORDER", "VISUALIZATION_TRANSITION_MS"]
    - path: "src/visualization/selectors/buildVisualizationModel.ts"
      provides: "normalized phase/zone series and latest zone risk map from SimulationOutput"
      exports: ["buildVisualizationModel"]
    - path: "src/visualization/contracts/motionPolicy.ts"
      provides: "shared animation policy for animejs/react-kino integrations"
      exports: ["VISUALIZATION_TRANSITION_MS", "VISUALIZATION_EASE", "PREFERS_REDUCED_MOTION"]
    - path: "src/hooks/useScenarioStore.ts"
      provides: "persistent scenario state plus latestSimulationOutput for visualization consumers"
      contains: "latestSimulationOutput"
  key_links:
    - from: "src/components/config/ScenarioForm.tsx"
      to: "src/hooks/useScenarioStore.ts"
      via: "Run handler writes latestSimulationOutput"
      pattern: "setLatestSimulationOutput\(result\)"
    - from: "src/visualization/selectors/buildVisualizationModel.ts"
      to: "src/visualization/contracts/riskEncoding.ts"
      via: "shared threshold mapping"
      pattern: "riskBandFromRatio"
    - from: "src/visualization/contracts/motionPolicy.ts"
      to: "src/visualization/components/RiskLineChart.tsx"
      via: "animejs transition policy"
      pattern: "VISUALIZATION_TRANSITION_MS|VISUALIZATION_EASE"
---

<objective>
Create the shared visualization contracts and data pipeline so all Phase 3 views consume one normalized simulation model with locked severity semantics.

Purpose: Eliminate threshold drift and establish a single source of truth before rendering D3 chart and polygon heatmap.
Output: Installed D3 + animation dependencies, centralized risk/motion policy modules, normalized selector, and state plumbing from Run action into visualization data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decision-grade-risk-visualization/03-CONTEXT.md
@.planning/phases/03-decision-grade-risk-visualization/03-RESEARCH.md
@src/components/config/ScenarioForm.tsx
@src/hooks/useScenarioStore.ts
@src/simulation/contracts/output.schema.ts
@.vscode/mcp.json

<interfaces>
From src/simulation/contracts/output.schema.ts:
```ts
export type SimulationOutput = z.infer<typeof SimulationOutputSchema>
// Key rows used by visualization prep:
// phaseZoneMatrix[]: { phaseId, zoneId, occupancyRatio, occupancySeverity, ... }
// peakSummaries[]: { zoneId, phaseId, peakOccupancyRatio, peakSeverity }
```

From src/hooks/useScenarioStore.ts (existing):
```ts
type ScenarioState = {
  currentInput: SimulationInput
  savedScenarios: Record<string, SimulationInput>
  updateInput(input: SimulationInput): void
  saveScenario(name: string, input?: SimulationInput): void
  loadScenario(name: string): void
  applyPreset(preset: PresetName): void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish shared risk policy and visualization model contracts</name>
  <files>package.json, package-lock.json, .vscode/mcp.json, src/visualization/contracts/riskEncoding.ts, src/visualization/contracts/motionPolicy.ts, src/visualization/selectors/buildVisualizationModel.ts, tests/ui/fixtures/simulationOutput.ts</files>
  <behavior>
    - Test 1: Ratios map to bands exactly per D-05: green for <0.80, amber for 0.80-0.95, red for >=0.95.
    - Test 2: Transition constants are exported and stay within D-08 range (150-250ms).
    - Test 3: Selector output contains deterministic phase ordering and zone series keyed by zoneId.
    - Test 4: Motion policy supports reduced-motion fallback and is reusable by animejs/react-kino integrations.
  </behavior>
  <action>Install `d3`, `animejs`, and `react-kino` dependencies (per UX tool integration). Confirm `.vscode/mcp.json` includes a running shadcn MCP server config for component generation workflow. Add `riskEncoding.ts` with locked threshold constants, band labels, and legend metadata used by both chart and heatmap (per D-05, D-06). Add `motionPolicy.ts` with `VISUALIZATION_TRANSITION_MS` constrained to 150-250ms plus shared easing/reduced-motion guards for animejs/react-kino consumers (per D-08). Add `buildVisualizationModel.ts` to derive chart series + latest zone ratios from `SimulationOutput.phaseZoneMatrix` using stable sorting/tie-breaks by `zoneId` for deterministic top-5 behavior (supports D-02). Add deterministic fixture(s) in `tests/ui/fixtures/simulationOutput.ts` for downstream UI tests.</action>
  <verify>
    <automated>npm ls d3 animejs react-kino --depth=0</automated>
  </verify>
  <done>D3 + animation dependencies are installed, shared risk/motion contracts exist, and normalized visualization model utilities compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist latest simulation output into UI state for visualization consumers</name>
  <files>src/hooks/useScenarioStore.ts, src/components/config/ScenarioForm.tsx, tests/ui/store.test.ts, tests/ui/run.test.ts</files>
  <behavior>
    - Test 1: Clicking Run with valid input writes latest simulation output into store.
    - Test 2: Invalid input keeps latest simulation output unchanged.
    - Test 3: Existing preset/save/load behavior remains intact.
  </behavior>
  <action>Extend scenario store with `latestSimulationOutput` and `setLatestSimulationOutput` (null-safe). Update `ScenarioForm` run handler to persist simulation results to the store instead of console-only handling. Preserve explicit validation gate from Phase 2: no simulation call or store update on invalid input. Update existing store/run tests to assert new output persistence behavior and guard against regressions.</action>
  <verify>
    <automated>npm run test -- tests/ui/store.test.ts tests/ui/run.test.ts</automated>
  </verify>
  <done>Visualization pipeline has a reliable source of truth in state, fed by successful Run actions only.</done>
</task>

</tasks>

<verification>
Run focused contract and state checks:
- `npm ls d3 animejs react-kino --depth=0`
- `npm run test -- tests/ui/store.test.ts tests/ui/run.test.ts`
</verification>

<success_criteria>
- Shared threshold/legend/transition policy exists in one module and is ready for both views.
- Simulation Run writes latest output to state for downstream visualization rendering.
- No regression in existing scenario store and run-validation behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-grade-risk-visualization/03-01-SUMMARY.md`
</output>
