---
phase: 12-webpage-design-rehaul
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: 
  - "src/components/three-stadium.tsx"
  - "src/app/page.tsx"
autonomous: true
requirements: []

must_haves:
  truths:
    - "A 3D stadium or central visual scene renders on the homepage."
    - "The scene is interactive or animated using React Three Fiber."
  artifacts:
    - path: "src/components/three-stadium.tsx"
      provides: "The React Three Fiber Canvas and Scene"
    - path: "src/app/page.tsx"
      provides: "Landing page updated to include the stadium"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/three-stadium.tsx"
      via: "import ThreeStadium"
      pattern: "<ThreeStadium />"
---

<objective>
Implement the core 3D visual using Three.js / React Three Fiber.

Purpose: Fulfill the requirement for a central 3D visual (e.g. stadium) to anchor the modernized design.
Output: A Three.js Canvas component and its integration onto the homepage.
</objective>

<execution_context>
@D:/Codes/Personal/prompt-wars/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Codes/Personal/prompt-wars/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-webpage-design-rehaul/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 3D Stadium Component</name>
  <files>src/components/three-stadium.tsx</files>
  <read_first>src/components/ui/hero-card.tsx</read_first>
  <action>Create a new client component `src/components/three-stadium.tsx` using `@react-three/fiber` and `@react-three/drei`. Setup a `<Canvas>`, basic lights, `<OrbitControls />`, and a geometry representation of a "stadium" or similar central visual focus point. Make sure it has a responsive height (e.g. `h-[60vh]`).</action>
  <verify>
    <automated>grep "react-three/fiber" src/components/three-stadium.tsx</automated>
  </verify>
  <done>Component compiles with a `<Canvas>` and basic 3D scene.</done>
  <acceptance_criteria>
    - src/components/three-stadium.tsx imports "Canvas" from "@react-three/fiber"
    - Component renders a mesh
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integrate 3D Component on Home Page</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>Import `ThreeStadium` into `src/app/page.tsx` and place it prominently above or below the `CTASection`. Ensure layout structure supports a full-width or hero-sized 3D canvas.</action>
  <verify>
    <automated>grep "ThreeStadium" src/app/page.tsx</automated>
  </verify>
  <done>Landing page displays the 3D scene.</done>
  <acceptance_criteria>
    - src/app/page.tsx contains "<ThreeStadium"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client | Rendering of WebGL context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Denial of Service | three-stadium.tsx | mitigate | Keep polygon count low to prevent browser freezing on lower-end devices. |
</threat_model>

<verification>
App loads and displays the 3D canvas with interactive controls.
</verification>

<success_criteria>
- `three-stadium.tsx` is valid R3F syntax
- Rendered on the main page successfully
</success_criteria>

<output>
After completion, create `.planning/phases/12-webpage-design-rehaul/12-03-SUMMARY.md`
</output>