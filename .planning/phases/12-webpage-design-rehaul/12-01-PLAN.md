---
phase: 12-webpage-design-rehaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - "package.json"
  - "next.config.mjs"
  - "tailwind.config.ts"
  - "src/app/layout.tsx"
  - "src/app/globals.css"
  - "src/components/theme-provider.tsx"
autonomous: true
requirements: []

must_haves:
  truths:
    - "App is running Next.js + Vite correctly configured."
    - "Shadcn UI and tailwind are integrated with modular theme.css format."
  artifacts:
    - path: "package.json"
      provides: "Next.js and dependencies including three.js, shadcn"
    - path: "src/app/globals.css"
      provides: "Modular CSS variables based on shadcnthemer"
    - path: "src/app/layout.tsx"
      provides: "Root layout for multi-page application"
  key_links:
    - from: "src/app/globals.css"
      to: "tailwind.config.ts"
      via: "CSS variables"
      pattern: "var(--"
---

<objective>
Migrate project configuration to Next.js (with Vite if using custom builder/turbopack otherwise standard Next app router) and implement the baseline Shadcn UI with modular `theme.css`.

Purpose: Establish the structural and styling foundation for the webpage rehaul.
Output: Updated package dependencies, root layout, and global CSS theme.
</objective>

<execution_context>
@D:/Codes/Personal/prompt-wars/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Codes/Personal/prompt-wars/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-webpage-design-rehaul/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate configuration to Next.js and Tailwind baseline</name>
  <files>package.json, next.config.mjs, tailwind.config.ts</files>
  <read_first>package.json</read_first>
  <action>Ensure `next`, `react`, `react-dom`, `three`, `@react-three/fiber`, `@react-three/drei`, `lucide-react`, `tailwindcss`, `clsx`, `tailwind-merge` are in dependencies. Create/update `next.config.mjs` for app router. Configure `tailwind.config.ts` to use CSS variables for colors per shadcn standards.</action>
  <verify>
    <automated>npm ls next three tailwindcss</automated>
  </verify>
  <done>Dependencies are installed and tailwind config is set up.</done>
  <acceptance_criteria>
    - package.json contains "next", "three"
    - tailwind.config.ts contains "extend: { colors:"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Shadcn modular theme.css</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/components/theme-provider.tsx</files>
  <read_first>src/app/globals.css</read_first>
  <action>Rewrite `src/app/globals.css` from scratch to use the shadcnthemer.com format (e.g. `--background: 0 0% 100%;` and `.dark` blocks). Create `theme-provider.tsx` for next-themes. Update `src/app/layout.tsx` to wrap children in the ThemeProvider.</action>
  <verify>
    <automated>cat src/app/globals.css | grep -q "\-\-background"</automated>
  </verify>
  <done>Clean CSS is in place and layout wraps the app with theme context.</done>
  <acceptance_criteria>
    - src/app/globals.css contains ":root {" and ".dark {"
    - src/app/layout.tsx imports "ThemeProvider"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> UI | Browser executing local CSS and layout wrappers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | tailwind.config.ts | accept | Config files are committed, low risk. |
</threat_model>

<verification>
App compiles with Next.js and basic theming applies to the root layout.
</verification>

<success_criteria>
- package.json has required libs
- src/app/globals.css uses variable format
- src/app/layout.tsx is valid Next.js layout
</success_criteria>

<output>
After completion, create `.planning/phases/12-webpage-design-rehaul/12-01-SUMMARY.md`
</output>