---
phase: 12-webpage-design-rehaul
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: 
  - "src/components/ui/hero-card.tsx"
  - "src/app/page.tsx"
autonomous: true
requirements: []
user_setup:
  - service: "21st-dev magic"
    why: "Integration of magic MCP for open code UI"
    env_vars:
      - name: "API_KEY"
        source: "User provided: ac7e0868a00de91813bd5ae0f7d31dbd38ae7ceb9f28a5bd4ef5f0de2e003fd1"

must_haves:
  truths:
    - "Hero card displays on the homepage."
    - "Dithering effect from paper-design shaders works correctly."
  artifacts:
    - path: "src/components/ui/hero-card.tsx"
      provides: "The CTA hero section component"
    - path: "src/app/page.tsx"
      provides: "Main landing page"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ui/hero-card.tsx"
      via: "import CTASection"
      pattern: "<CTASection />"
---

<objective>
Implement the CTA Hero Card and 21st-dev Magic MCP integration on the landing page.

Purpose: Deliver the required Hero section featuring the text "Your words, delivered perfectly" and the dithering shader.
Output: `hero-card.tsx` component and updated `page.tsx`.
</objective>

<execution_context>
@D:/Codes/Personal/prompt-wars/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Codes/Personal/prompt-wars/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-webpage-design-rehaul/12-CONTEXT.md
@herocard.tsx.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Hero Card Component</name>
  <files>src/components/ui/hero-card.tsx</files>
  <read_first>herocard.tsx.example</read_first>
  <action>Create `src/components/ui/hero-card.tsx` mirroring the exact code from `herocard.tsx.example`. Ensure `@paper-design/shaders-react` is installed in `package.json` (add it via `npm install @paper-design/shaders-react`). Export `CTASection`.</action>
  <verify>
    <automated>grep "export function CTASection" src/components/ui/hero-card.tsx</automated>
  </verify>
  <done>Hero component matches example and dependencies exist.</done>
  <acceptance_criteria>
    - src/components/ui/hero-card.tsx contains "<Dithering"
    - package.json contains "@paper-design/shaders-react"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Assemble the Landing Page</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>Create or update the root `page.tsx` to import and render `<CTASection />`. Structure the page to leave space for the upcoming 3D visual section.</action>
  <verify>
    <automated>grep "CTASection" src/app/page.tsx</automated>
  </verify>
  <done>Landing page displays the hero card.</done>
  <acceptance_criteria>
    - src/app/page.tsx imports CTASection
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client | Shader rendering execution via WebGL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Info Disclosure | Magic API Key | mitigate | Store key in `.env.local` and never commit it, use placeholder in source. |
</threat_model>

<verification>
Loading `/` displays the hero card with the dithering shader.
</verification>

<success_criteria>
- `hero-card.tsx` compiles and runs
- Root page includes it
- `@paper-design/shaders-react` is installed
</success_criteria>

<output>
After completion, create `.planning/phases/12-webpage-design-rehaul/12-02-SUMMARY.md`
</output>