---
phase: 16-fan-chatbot
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/hooks/useChatStream.ts
  - src/components/fan/ChatInput.tsx
  - src/components/fan/QuickChips.tsx
  - src/components/fan/StreamingContent.tsx
  - tests/hooks/useChatStream.test.ts
autonomous: true
requirements:
  - FAN-01
user_setup: []
must_haves:
  truths:
    - "useChatStream.sendMessage() POSTs to /api/chat with messages and match state query params"
    - "useChatStream cancels in-flight request when new message is sent"
    - "useChatStream adds user message to chatSlice immediately"
    - "ChatInput renders text input + send button, disabled during streaming"
    - "QuickChips shows 4 quick-question buttons, hidden after first message"
    - "StreamingContent renders streaming text token-by-token with structure cards below"
  artifacts:
    - path: "src/hooks/useChatStream.ts"
      provides: "POST-based SSE client hook (fetch + ReadableStream)"
      exports: ["useChatStream"]
    - path: "src/components/fan/ChatInput.tsx"
      provides: "Text input with send button"
      exports: ["ChatInput"]
    - path: "src/components/fan/QuickChips.tsx"
      provides: "Quick question suggestion chips"
      exports: ["QuickChips"]
    - path: "src/components/fan/StreamingContent.tsx"
      provides: "Streaming text renderer with structured data cards"
      exports: ["StreamingContent"]
    - path: "tests/hooks/useChatStream.test.ts"
      provides: "Unit tests for useChatStream hook"
  key_links:
    - from: "src/hooks/useChatStream.ts"
      to: "src/stores/liveStore.ts"
      via: "useLiveStore selectors (addMessage, setStreaming, match)"
      pattern: "useLiveStore"
    - from: "src/components/fan/ChatInterface.tsx"
      to: "src/hooks/useChatStream.ts"
      via: "import { useChatStream }"
      pattern: "useChatStream"
---

<objective>
Client-side SSE hook and input components: useChatStream, ChatInput, QuickChips, StreamingContent (per D-16-07 stateless POST, D-16-11 quick chips, D-16-12 rich streaming + cards).

Purpose: Provide the reusable client infrastructure for chatting: POST-based SSE consumption via fetch, text input with send, quick chips, and streaming text + card rendering.
Output: Hook and three UI components ready for ChatInterface to wire together.
</objective>

<execution_context>
@/home/zeph/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/zeph/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-fan-chatbot/16-CONTEXT.md
@.planning/phases/16-fan-chatbot/16-RESEARCH.md
@.planning/phases/16-fan-chatbot/16-UI-SPEC.md
@.planning/phases/16-fan-chatbot/16-PATTERNS.md

# Reference implementations
@src/hooks/useAlertStream.ts
@src/stores/liveStore.ts
@src/stores/slices/chatSlice.ts
@src/types/chat.ts
@tests/hooks/useAlertStream.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test for useChatStream hook</name>
  <files>
    tests/hooks/useChatStream.test.ts
  </files>
  <read_first>
    @tests/hooks/useAlertStream.test.ts — Reference test pattern for SSE hooks
    @src/hooks/useAlertStream.ts — Reference EventSource pattern (useChatStream uses fetch instead)
    @src/types/chat.ts — Types used by hook
  </read_first>
  <behavior>
    - Test 1: POSTs to /api/chat with correct match state query params
    - Test 2: Adds user message to store immediately on send
    - Test 3: Sets isStreaming=true while response is streaming
    - Test 4: Sets isStreaming=false when stream completes
    - Test 5: Aborts in-flight request when new message is sent
    - Test 6: Handles AbortError without propagating to consumer
  </behavior>
  <action>
    Create tests/hooks/useChatStream.test.ts:

    IMPORT from react: renderHook, act, waitFor from @testing-library/react
    IMPORT useChatStream from @/hooks/useChatStream
    IMPORT useLiveStore from @/stores/liveStore

    MOCK global.fetch before each test — return a mock Response with a ReadableStream that simulates SSE events:
    - Mock SSE data: `data: {"type":"token","text":"Hello"}\n\ndata: {"type":"token","text":" world"}\n\ndata: {"type":"complete"}\n\n`
    - Mock ok: true, body: readableStream
    - Track fetch calls with vi.fn()

    SETUP: Need to mock the Zustand store. Use a simple approach:
    - Before each test, reset the store state
    - Import liveStore directly and call setState to reset
    - Or create a fresh store for testing

    TEST CASES (6 total):
    1. "POSTs to /api/chat with match state query params" — call sendMessage("Where's my gate?", []), verify fetch called with /api/chat?minute=...&phase=...&score=... URL including all match params
    2. "adds user message to chatSlice immediately" — check store.messages includes user message after sendMessage call
    3. "sets isStreaming=true while response is streaming" — check store.isStreaming is true during stream
    4. "sets isStreaming=false when stream completes" — wait for stream to finish, check isStreaming is false
    5. "aborts in-flight request on new message" — call sendMessage twice, verify first AbortController.abort() called
    6. "handles AbortError without propagating" — mock fetch to reject with AbortError, call sendMessage, verify no unhandled error

    Use vi.useFakeTimers() if needed for timing control.
    Use vi.stubGlobal("fetch", mockFetch) pattern.
  </action>
  <verify>
    <automated>npx vitest run tests/hooks/useChatStream.test.ts 2>&1 | head -30</automated>
  </verify>
  <done>
    - tests/hooks/useChatStream.test.ts exists with 6 test cases
    - Tests compile (may fail until hook implementation is done)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useChatStream hook, ChatInput, QuickChips, StreamingContent</name>
  <files>
    src/hooks/useChatStream.ts
    src/components/fan/ChatInput.tsx
    src/components/fan/QuickChips.tsx
    src/components/fan/StreamingContent.tsx
  </files>
  <read_first>
    @src/hooks/useAlertStream.ts — EventSource-based SSE hook (reference for store interaction)
    @src/types/chat.ts — ChatMessage, ChatResponse types
    @src/stores/slices/chatSlice.ts — addMessage, setStreaming, messages, isStreaming
    @src/components/ui/button.tsx — shadcn Button component API
    @src/components/ui/input.tsx — shadcn Input component API
  </read_first>
  <behavior>
    - Test 1-6: All 6 hook tests pass
  </behavior>
  <action>
    CREATE src/hooks/useChatStream.ts (per D-16-07 stateless — client sends messages with POST):
    "use client";
    - Import: useCallback, useRef from react
    - Import: useLiveStore from @/stores/liveStore
    - Import: ChatMessage, ChatResponse from @/types/chat
    - Export useChatStream function returning { sendMessage, cancelStream }
    - sendMessage(content, history) implementation per RESEARCH.md Example 4:
      * Cancel any in-flight request via abortRef.current.abort()
      * Create new AbortController
      * Add user message to store
      * Add empty assistant message placeholder to store
      * Set isStreaming=true
      * Build URL with match state query params from liveStore match
      * POST to /api/chat with { messages: history.concat([userMsg]).slice(-10) }
      * Read SSE stream via response.body.getReader()
      * Parse SSE events line-by-line (handle chunk boundary reassembly)
      * On "token" events: accumulate content (store updates handled via slice or direct)
      * On "structured" event: store the structured data for rendering
      * On "complete" event: finalize
      * On error: log and let UI show error state
      * AbortError: silently return (do not propagate)
      * finally: setStreaming(false), clear abortRef
    - cancelStream() implementation: abort if in-flight, clear ref

    CREATE src/components/fan/ChatInput.tsx:
    "use client";
    - Import: useState, useRef, useEffect from react
    - Import: Input from @/components/ui/input
    - Import: Button from @/components/ui/button
    - Import: Send from lucide-react
    - Props: { onSend: (text: string) => void, disabled: boolean }
    - State: text (string)
    - Auto-focus input when not disabled via useEffect
    - Submit on Enter key or button click
    - Clear input on submit
    - Send button disabled when text is empty or disabled prop is true
    - Styling per UI-SPEC §Spacing: md (16px) gap, flex row layout

    CREATE src/components/fan/QuickChips.tsx (per D-16-11 quick chips before first message only):
    "use client";
    - Import: Button from @/components/ui/button
    - Props: { onSelect: (question: string) => void }
    - 4 quick question constants (per D-16-11 discretion):
      * "Where's my gate?"
      * "Best exit after the match?"
      * "Where's the nearest concession?"
      * "How crowded is my section?"
    - Render as flex-wrap centered flexbox of outline rounded-full buttons
    - Styling per UI-SPEC §Typography: label 13px weight 600

    CREATE src/components/fan/StreamingContent.tsx (per D-16-12 rich streaming + structured cards):
    "use client";
    - Import: Card, CardContent from @/components/ui/card
    - Import: Badge from @/components/ui/badge
    - Import: ChatResponse from @/types/chat
    - Props: { text: string, structured?: ChatResponse | null }
    - Render text as whitespace-pre-wrap paragraph
    - If structured object exists with suggestedGate:
      * Render Card with left accent border (var(--primary)) showing gate + walking time
    - If structured.zoneInfo exists:
      * Render Badge with zone name + "X% full"
    - Styling per UI-SPEC: body 15px weight 400, card spacing md
  </action>
  <verify>
    <automated>npx vitest run tests/hooks/useChatStream.test.ts</automated>
  </verify>
  <done>
    - useChatStream hook works: POSTs to /api/chat, handles SSE stream, supports abort
    - ChatInput renders with input + send button, disabled during streaming
    - QuickChips renders 4 quick questions, calls onSelect
    - StreamingContent renders text + structured data cards
    - All 6 hook tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→API | fetch() POST to /api/chat — match state params sent as query string (minute, phase, score — low sensitivity) |
| API→Browser | SSE events parsed via JSON.parse — trusted (same origin, server-validated) |
| Component state | chatSlice stores ephemeral messages in Zustand (in-memory only, no localStorage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | useChatStream URL query params | mitigate | Match state params are match metadata (minute, phase, score, teams) — not sensitive. No env vars or keys sent client-side. |
| T-16-06 | Denial of Service | AbortController cleanup | mitigate | abortRef tracks in-flight requests. cancelStream() on unmount prevents resource leaks. Each new message aborts previous request. |
</threat_model>

<verification>
- npx vitest run tests/hooks/useChatStream.test.ts passes all 6 tests
</verification>

<success_criteria>
- useChatStream hook POSTs to /api/chat and parses SSE events
- ChatInput renders with proper disabled/enabled states
- QuickChips renders 4 chips that trigger onSelect
- StreamingContent renders text + structured cards
- Hook tests all pass
</success_criteria>

<output>
Create `.planning/phases/16-fan-chatbot/16-03-SUMMARY.md` when done
</output>
