---
phase: 16-fan-chatbot
plan: 04
type: execute
wave: 3
depends_on:
  - 16-03
files_modified:
  - src/components/fan/ChatMessage.tsx
  - src/components/fan/ChatInterface.tsx
  - src/app/(fan)/layout.tsx
  - src/app/(fan)/page.tsx
  - tests/components/fan/ChatMessage.test.tsx
  - tests/components/fan/ChatInterface.test.tsx
autonomous: true
requirements:
  - FAN-01
user_setup: []
must_haves:
  truths:
    - "Fan can navigate to /fan route and see a chatbot interface with message input"
    - "ChatMessage renders user bubbles (right-aligned) and assistant bubbles (left-aligned)"
    - "Streaming cursor animated dot visible while isStreaming is true"
    - "Quick question chips shown before first message only"
    - "Quick question chips hidden after first user message"
    - "New conversation button resets to empty state (chips visible again)"
    - "Fan layout includes MagneticDock for full site navigation"
  artifacts:
    - path: "src/components/fan/ChatMessage.tsx"
      provides: "Chat message bubble component with role-based styling"
      exports: ["ChatMessage"]
    - path: "src/components/fan/ChatInterface.tsx"
      provides: "Full-page chat layout composing all fan components"
      exports: ["ChatInterface"]
    - path: "src/app/(fan)/layout.tsx"
      provides: "Fan route group layout with MagneticDock"
      exports: ["default"]
    - path: "src/app/(fan)/page.tsx"
      provides: "/fan page rendering ChatInterface"
      exports: ["default"]
    - path: "tests/components/fan/ChatMessage.test.tsx"
      provides: "Unit tests for ChatMessage rendering states"
    - path: "tests/components/fan/ChatInterface.test.tsx"
      provides: "Unit tests for ChatInterface states"
  key_links:
    - from: "src/app/(fan)/page.tsx"
      to: "src/components/fan/ChatInterface.tsx"
      via: "import { ChatInterface }"
      pattern: "ChatInterface"
    - from: "src/app/(fan)/layout.tsx"
      to: "src/components/layout/MagneticDock.tsx"
      via: "import { MagneticDock }"
      pattern: "MagneticDock"
    - from: "src/components/fan/ChatInterface.tsx"
      to: "src/stores/liveStore.ts"
      via: "useLiveStore selectors (messages, isStreaming, clearMessages)"
      pattern: "useLiveStore"
    - from: "src/components/fan/ChatInterface.tsx"
      to: "src/hooks/useChatStream.ts"
      via: "import { useChatStream }"
      pattern: "useChatStream"
---

<objective>
Chat message component, full-page chat interface, fan route group layout and page (per D-16-10 full-page layout, D-16-12 rich streaming cards, D-16-13 route group, D-16-14 MagneticDock navigation).

Purpose: Wire all fan chatbot components together into a /fan page with MagneticDock navigation, composed through ChatInterface.
Output: Working /fan page with full chat functionality.
</objective>

<execution_context>
@/home/zeph/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/zeph/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-fan-chatbot/16-CONTEXT.md
@.planning/phases/16-fan-chatbot/16-UI-SPEC.md
@.planning/phases/16-fan-chatbot/16-PATTERNS.md

# Reference implementations
@src/app/(dashboard)/layout.tsx
@src/app/(dashboard)/dashboard/page.tsx
@src/components/dashboard/AlertFeed.tsx
@src/components/layout/MagneticDock.tsx
@src/hooks/useChatStream.ts
@src/types/chat.ts
@src/stores/slices/chatSlice.ts
@tests/components/dashboard/AlertFeed.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests for ChatMessage and ChatInterface components</name>
  <files>
    tests/components/fan/ChatMessage.test.tsx
    tests/components/fan/ChatInterface.test.tsx
  </files>
  <read_first>
    @tests/components/dashboard/AlertFeed.test.tsx — Pattern for component tests with vitest + @testing-library/react
    @src/components/dashboard/AlertFeed.tsx — Reference for streaming rendering test structure
  </read_first>
  <behavior>
    - Test 1: ChatMessage renders user bubble with role="user" on right
    - Test 2: ChatMessage renders assistant bubble with role="assistant" on left
    - Test 3: ChatMessage shows streaming cursor when isStreaming=true
    - Test 4: ChatMessage does not show streaming cursor when isStreaming=false
    - Test 5: ChatInterface shows welcome heading and quick chips when no messages
    - Test 6: ChatInterface hides quick chips after first message
    - Test 7: ChatInterface shows "New conversation" button when messages exist
  </behavior>
  <action>
    CREATE tests/components/fan/ChatMessage.test.tsx:
    - Import render, screen from @testing-library/react
    - Import ChatMessage from @/components/fan/ChatMessage
    - Import ChatMessage type from @/types/chat
    - Create mock messages with id, role, content, timestamp
    - Test 1: render with role "user", expect "justify-end" class (right-aligned)
    - Test 2: render with role "assistant", expect "justify-start" class (left-aligned)
    - Test 3: render with isStreaming=true, expect animated cursor element
    - Test 4: render with isStreaming=false, expect no cursor element
    - Use screen.getByText to verify content is rendered

    CREATE tests/components/fan/ChatInterface.test.tsx:
    - Import render, screen, fireEvent from @testing-library/react
    - Import ChatInterface from @/components/fan/ChatInterface
    - Need to mock useChatStream and the Zustand store
    - Mock useChatStream: return { sendMessage: vi.fn(), cancelStream: vi.fn() }
    - Mock useLiveStore selectors:
      * Default (empty state): messages=[], isStreaming=false, clearMessages=vi.fn()
    - Test 1: empty state shows welcome heading per UI-SPEC copy ("Welcome to the Stadium Assistant")
    - Test 2: empty state shows quick chips ("Where's my gate?" visible)
    - Test 3: quick chips call onSelect when clicked (integration with sendMessage)
    - Test 4: "New conversation" button visible when messages exist
    - Test 5: Quick chips hidden when messages exist

    For store mocking, use vi.mock("@/stores/liveStore") to return selectors.
    Reset mocks in beforeEach.
  </action>
  <verify>
    <automated>npx vitest run tests/components/fan/ChatMessage.test.tsx tests/components/fan/ChatInterface.test.tsx 2>&1 | head -40</automated>
  </verify>
  <done>
    - Both test files exist with ≥4 test cases each
    - Tests compile (may fail until components are implemented)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ChatMessage and ChatInterface components</name>
  <files>
    src/components/fan/ChatMessage.tsx
    src/components/fan/ChatInterface.tsx
  </files>
  <read_first>
    @src/types/chat.ts — ChatMessage type
    @src/hooks/useChatStream.ts — sendMessage, cancelStream API
    @src/stores/slices/chatSlice.ts — messages, isStreaming, clearMessages, addMessage
    @src/components/dashboard/AlertFeed.tsx — Reference for scrolling and state patterns
  </read_first>
  <behavior>
    - Test 1-7: All component tests pass
  </behavior>
  <action>
    CREATE src/components/fan/ChatMessage.tsx:
    "use client";
    - Import cn from @/lib/utils
    - Import type { ChatMessage as ChatMessageType } from @/types/chat
    - Props: { message: ChatMessageType, isStreaming?: boolean }
    - Render as div with flex + justify-end (user) or justify-start (assistant)
    - User bubbles: bg-primary text-primary-foreground, rounded-2xl
    - Assistant bubbles: bg-muted text-foreground, rounded-2xl
    - Message text: text-sm whitespace-pre-wrap
    - If isStreaming: inline-block w-2 h-4 animate-pulse cursor
    - Styling per UI-SPEC §Color + §Typography: body 15px/400/1.5

    CREATE src/components/fan/ChatInterface.tsx:
    "use client";
    - Import: useState, useRef, useEffect from react
    - Import: useLiveStore from @/stores/liveStore
    - Import: useChatStream from @/hooks/useChatStream
    - Import: ChatMessage from @/components/fan/ChatMessage
    - Import: ChatInput from @/components/fan/ChatInput
    - Import: QuickChips from @/components/fan/QuickChips
    - Import: StreamingContent from @/components/fan/StreamingContent
    - Import: Button from @/components/ui/button
    - Import: MessageSquare from lucide-react
    - State: hasSentMessage (boolean, defaults false)
    - Refs: scrollRef for auto-scrolling message list
    - Selectors from useLiveStore: messages, isStreaming, clearMessages
    - Hook: const { sendMessage, cancelStream } = useChatStream()

    Layout per UI-SPEC §Route & Layout (per D-16-10 full-page layout):
    - Full-height div (h-screen), centered max-w-3xl, flex-col
    - Header: "Stadium Assistant" title + "New conversation" button (secondary ghost, per D-16-09 reset)
      * New conversation button hidden during streaming
      * Calls clearMessages on click
    - Scrollable message area (flex-1 overflow-y-auto)
      * Empty state: MessageSquare icon + "Welcome to the Stadium Assistant" heading + "Ask me about gates, exits, concessions, and crowd levels during the match." body (per UI-SPEC copywriting contract)
      * Map messages to ChatMessage component
      * QuickChips shown only when messages.length === 0 AND !hasSentMessage
      * Auto-scroll on new messages via useEffect + scrollRef
    - Input area (border-t, p-4):
      * ChatInput with onSend handler
      * On send: setHasSentMessage(true), sendMessage(text, messages)
      * Disabled during isStreaming
    - Streaming cursor visibility: pass isStreaming to the last assistant message
    - useEffect for cancelStream on unmount cleanup
    - useEffect for auto-scroll whenever messages change
  </action>
  <verify>
    <automated>npx vitest run tests/components/fan/ChatMessage.test.tsx tests/components/fan/ChatInterface.test.tsx</automated>
  </verify>
  <done>
    - ChatMessage renders user/assistant bubbles with correct alignment and styling
    - ChatInterface shows welcome state, active state, streaming state
    - Quick chips visible before first message, hidden after
    - New conversation button resets all state
    - All 7 component tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Create fan route group layout and page</name>
  <files>
    src/app/(fan)/layout.tsx
    src/app/(fan)/page.tsx
  </files>
  <read_first>
    @src/app/(dashboard)/layout.tsx — Exact pattern for route group layout (parallel structure)
    @src/app/(dashboard)/dashboard/page.tsx — Reference page structure
    @src/components/layout/MagneticDock.tsx — Navigation component (check activeTab prop)
  </read_first>
  <action>
    CREATE src/app/(fan)/layout.tsx (per D-16-13 route group + D-16-14 MagneticDock):
    "use client";
    - Import MagneticDock from @/components/layout/MagneticDock
    - Layout per PATTERNS.md FanLayout pattern:
      * div flex min-h-screen flex-col
      * main flex-1 with {children}
      * MagneticDock at the bottom with activeTab="overview" (no "fan" tab exists — use closest match)
    - Match the app/(dashboard)/layout.tsx pattern for route group convention

    NOTE: MagneticDock currently has tabs: overview, simulate, compare, report, deploy.
    The "fan" page is outside the dock (it's a separate route group).
    Use activeTab="overview" as default, or best match.
    Keep same structure as dashboard layout but without the MatchBanner header.

    CREATE src/app/(fan)/page.tsx (per D-16-13 route group):
    "use client";
    - Import ChatInterface from @/components/fan/ChatInterface
    - Simple page component that renders <ChatInterface />
    - Per PATTERNS.md FanPage pattern

    No special styling on page level — ChatInterface is full-page.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/\(fan\)/layout.tsx src/app/\(fan\)/page.tsx</automated>
  </verify>
  <done>
    - app/(fan)/layout.tsx wraps fan pages with MagneticDock navigation
    - app/(fan)/page.tsx renders ChatInterface
    - Both files compile without TypeScript errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR→Browser | Fan layout rendered by Next.js server — no user-specific data |
| Browser→API | ChatInterface sends messages through useChatStream hook |
| Component render | ChatMessage displays content from chatSlice — content is pre-validated by server-side Zod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Information Disclosure | ChatInterface welcome text | accept | Welcome copy is static marketing text, no sensitive info. per UI-SPEC copywriting contract |
| T-16-08 | Spoofing | "New conversation" button | accept | Non-destructive action — clears local state only. No server-side effects. |
</threat_model>

<verification>
- npx vitest run tests/components/fan/ChatMessage.test.tsx tests/components/fan/ChatInterface.test.tsx passes
- npx tsc --noEmit src/app/\(fan\)/layout.tsx src/app/\(fan\)/page.tsx passes
- Navigate to /fan route and verify chat UI renders (manual verify)
</verification>

<success_criteria>
- /fan route loads full-page chat UI with welcome message and quick chips
- User can type in input and see bubbles appear
- Streaming cursor visible during response generation
- Quick chips hidden after first user message
- New conversation button resets to empty state
- MagneticDock navigation visible at bottom of fan page
- All component tests pass
</success_criteria>

<output>
Create `.planning/phases/16-fan-chatbot/16-04-SUMMARY.md` when done
</output>
