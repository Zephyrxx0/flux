---
phase: 04-structured-ai-risk-reporting
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/reporting/components/RiskReportPanel.tsx
  - src/reporting/components/ReportSections.tsx
  - src/visualization/components/VisualizationWorkspace.tsx
  - src/components/layout/AppLayout.tsx
  - tests/ui/reporting/riskReportPanel.test.tsx
  - tests/ui/reporting/reportingFlow.test.tsx
autonomous: true
requirements: [AI-01, AI-02, AI-03]
must_haves:
  truths:
    - "Users see structured report content generated from current run output."
    - "AI failures are visible inline while deterministic fallback remains available."
    - "Retry is user-driven and does not block chart/heatmap readability."
  artifacts:
    - path: "src/reporting/components/RiskReportPanel.tsx"
      provides: "rendering surface for AI/fallback report states and Retry UX"
      exports: ["RiskReportPanel"]
    - path: "tests/ui/reporting/reportingFlow.test.tsx"
      provides: "end-to-end run -> report -> failure fallback -> retry coverage"
  key_links:
    - from: "src/visualization/components/VisualizationWorkspace.tsx"
      to: "src/reporting/components/RiskReportPanel.tsx"
      via: "shared workspace composition"
      pattern: "visualization-workspace|risk-report"
    - from: "src/reporting/components/RiskReportPanel.tsx"
      to: "src/hooks/useRiskReportStore.ts"
      via: "status-driven render and retry action"
      pattern: "retryReportGeneration|status"
---

<objective>
Integrate the reporting UX into the main workspace and finalize resilient user-facing behavior.

Purpose: Complete Phase 4 user value by making structured report generation visible, reliable, and actionable in the app.
Output: Risk report panel UI, integrated flow tests, and failure/retry experience aligned to context decisions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-structured-ai-risk-reporting/04-CONTEXT.md
@.planning/phases/04-structured-ai-risk-reporting/04-02-PLAN.md
@src/visualization/components/VisualizationWorkspace.tsx
@src/components/layout/AppLayout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build report panel with structured sections and inline failure handling</name>
  <files>src/reporting/components/RiskReportPanel.tsx, src/reporting/components/ReportSections.tsx, tests/ui/reporting/riskReportPanel.test.tsx</files>
  <read_first>src/hooks/useRiskReportStore.ts, src/reporting/contracts/riskReport.schema.ts</read_first>
  <action>Implement `RiskReportPanel` that renders all mandatory report sections in a briefing-friendly layout and displays explicit inline failure state with deterministic fallback notice. Add Retry button bound to explicit retry action. Ensure panel differentiates AI-generated vs fallback provenance while preserving identical section structure.</action>
  <verify>npm run test -- tests/ui/reporting/riskReportPanel.test.tsx</verify>
  <acceptance_criteria>
    - Panel always renders schema-defined sections when report data exists.
    - Failure messaging appears inline and does not hide fallback report content.
    - Retry button is visible/enabled only in appropriate states.
  </acceptance_criteria>
  <done>Report panel behavior is aligned with locked Phase 4 UX decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate panel into workspace flow and validate end-to-end behavior</name>
  <files>src/visualization/components/VisualizationWorkspace.tsx, src/components/layout/AppLayout.tsx, tests/ui/reporting/reportingFlow.test.tsx</files>
  <read_first>src/components/config/ScenarioForm.tsx, src/hooks/useScenarioStore.ts</read_first>
  <action>Compose the report panel into main workspace so it updates automatically after successful runs and coexists with chart/heatmap content. Add end-to-end UI tests covering: run success with AI report, run failure with fallback report, and user-triggered retry. Verify visualization surfaces remain readable and mounted during report failure states.</action>
  <verify>npm run test -- tests/ui/reporting/reportingFlow.test.tsx</verify>
  <acceptance_criteria>
    - Successful run leads to populated report panel tied to current simulation output.
    - AI failure still yields fallback report and preserves visualization content.
    - Retry action initiates a new AI attempt without resetting scenario form state.
  </acceptance_criteria>
  <done>Phase 4 delivers complete user-visible structured reporting with resilient fallback UX.</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/ui/reporting/riskReportPanel.test.tsx tests/ui/reporting/reportingFlow.test.tsx`
- `npm run test`
- `npm run typecheck`
</verification>

<success_criteria>
- Structured report panel is integrated and usable from the existing run workflow.
- Failure handling is transparent, non-blocking, and always accompanied by deterministic fallback content.
- Retry behavior is explicit and consistent with one-call-per-run baseline policy.
</success_criteria>

<output>
After completion, create `.planning/phases/04-structured-ai-risk-reporting/04-03-SUMMARY.md`
</output>
