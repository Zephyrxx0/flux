---
phase: 04-structured-ai-risk-reporting
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/reporting/gemini/buildPrompt.ts
  - src/reporting/gemini/generateRiskReport.ts
  - src/reporting/gemini/errors.ts
  - src/hooks/useRiskReportStore.ts
  - src/hooks/useScenarioStore.ts
  - tests/ui/reporting/generateRiskReport.test.ts
  - tests/ui/reporting/useRiskReportStore.test.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03]
must_haves:
  truths:
    - "One AI call is attempted per simulation run by default."
    - "AI responses are schema-validated before state update."
    - "Failure state transitions preserve usable fallback-ready context."
  artifacts:
    - path: "src/reporting/gemini/generateRiskReport.ts"
      provides: "Gemini adapter with strict parse gate and typed error mapping"
      exports: ["generateRiskReport", "isTransientGeminiError"]
    - path: "src/hooks/useRiskReportStore.ts"
      provides: "report lifecycle state machine and explicit Retry action"
      exports: ["useRiskReportStore"]
  key_links:
    - from: "src/hooks/useScenarioStore.ts"
      to: "src/hooks/useRiskReportStore.ts"
      via: "run completion triggers report generation request"
      pattern: "latestSimulationOutput"
    - from: "src/reporting/gemini/generateRiskReport.ts"
      to: "src/reporting/contracts/riskReport.schema.ts"
      via: "schema parse gate"
      pattern: "RiskReportSchema.safeParse|parse"
---

<objective>
Implement the Gemini report generation adapter and report lifecycle state management.

Purpose: Deliver AI-backed structured reports while preserving strict schema safety and deterministic failure handoff.
Output: Prompt builder, Gemini client adapter, typed error mapping, and report state store.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-structured-ai-risk-reporting/04-CONTEXT.md
@.planning/phases/04-structured-ai-risk-reporting/04-01-PLAN.md
@src/hooks/useScenarioStore.ts
@src/simulation/contracts/output.schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Gemini adapter with strict parse and one-call-per-run policy</name>
  <files>src/reporting/gemini/buildPrompt.ts, src/reporting/gemini/generateRiskReport.ts, src/reporting/gemini/errors.ts, tests/ui/reporting/generateRiskReport.test.ts</files>
  <read_first>src/reporting/contracts/riskReport.schema.ts, .planning/research/STACK.md</read_first>
  <action>Implement report prompt construction grounded in simulation JSON and call Gemini once per run. Parse only JSON payloads against `RiskReportSchema`. Map malformed output, timeout, and API errors into typed error classes with transient vs non-transient classification. Do not auto-retry on run path; expose transient detection for explicit Retry action only.</action>
  <verify>npm run test -- tests/ui/reporting/generateRiskReport.test.ts</verify>
  <acceptance_criteria>
    - AI payload that fails schema parse is rejected and classified as invalid output.
    - Adapter does not emit partial report state.
    - One-call-per-run behavior is enforced in the default generation path.
  </acceptance_criteria>
  <done>Gemini integration is schema-safe and deterministic in lifecycle behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add report lifecycle state store with explicit Retry action</name>
  <files>src/hooks/useRiskReportStore.ts, src/hooks/useScenarioStore.ts, tests/ui/reporting/useRiskReportStore.test.ts</files>
  <read_first>src/hooks/useScenarioStore.ts, src/reporting/fallback/buildDeterministicRiskReport.ts</read_first>
  <action>Create a dedicated report store (or scoped slice) with statuses: idle, loading, success, fallback, error. Trigger generation when `latestSimulationOutput` changes after successful run. On failure, immediately attach deterministic fallback report and keep error metadata for inline UX. Provide explicit `retryReportGeneration()` action that performs a user-initiated new AI attempt.</action>
  <verify>npm run test -- tests/ui/reporting/useRiskReportStore.test.ts</verify>
  <acceptance_criteria>
    - State transitions cover success and failure without clearing simulation output.
    - Failure path always includes fallback report payload.
    - Retry action is explicit and does not execute automatically.
  </acceptance_criteria>
  <done>Report lifecycle state is robust and ready for panel rendering.</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/ui/reporting/generateRiskReport.test.ts tests/ui/reporting/useRiskReportStore.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Gemini adapter enforces strict schema parsing for AI output.
- Run lifecycle triggers report generation automatically after valid simulation output.
- Failure state carries deterministic fallback and explicit Retry capability.
</success_criteria>

<output>
After completion, create `.planning/phases/04-structured-ai-risk-reporting/04-02-SUMMARY.md`
</output>
