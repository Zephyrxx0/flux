---
phase: 04-structured-ai-risk-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/reporting/contracts/riskReport.schema.ts
  - src/reporting/fallback/buildDeterministicRiskReport.ts
  - src/reporting/fallback/rules.ts
  - src/reporting/index.ts
  - tests/ui/reporting/riskReportSchema.test.ts
  - tests/ui/reporting/fallbackReport.test.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03]
must_haves:
  truths:
    - "AI and fallback reports share one strict schema contract with mandatory sections."
    - "Malformed AI output cannot reach render/export consumers."
    - "Fallback report generation is deterministic from simulation output only."
  artifacts:
    - path: "src/reporting/contracts/riskReport.schema.ts"
      provides: "single schema-valid contract for report parse and fallback output"
      exports: ["RiskReportSchema", "RiskReportSectionKeys", "RiskReport"]
    - path: "src/reporting/fallback/buildDeterministicRiskReport.ts"
      provides: "deterministic fallback report builder from simulation output"
      exports: ["buildDeterministicRiskReport"]
    - path: "tests/ui/reporting/fallbackReport.test.ts"
      provides: "deterministic fallback behavior and mandatory section coverage tests"
  key_links:
    - from: "src/simulation/contracts/output.schema.ts"
      to: "src/reporting/fallback/buildDeterministicRiskReport.ts"
      via: "grounding on phaseZoneMatrix and peakSummaries"
      pattern: "phaseZoneMatrix|peakSummaries|invariants"
    - from: "src/reporting/fallback/buildDeterministicRiskReport.ts"
      to: "src/reporting/contracts/riskReport.schema.ts"
      via: "schema parse gate before return"
      pattern: "RiskReportSchema.parse"
---

<objective>
Lock the report contract and deterministic fallback foundation for Phase 4.

Purpose: Ensure every report path (AI or fallback) uses the same strict structure so downstream UI and exports remain reliable.
Output: Strict report schema module, deterministic fallback builder, and contract tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-structured-ai-risk-reporting/04-CONTEXT.md
@src/simulation/contracts/output.schema.ts
@src/hooks/useScenarioStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict report schema and mandatory section contract</name>
  <files>src/reporting/contracts/riskReport.schema.ts, src/reporting/index.ts, tests/ui/reporting/riskReportSchema.test.ts</files>
  <read_first>src/simulation/contracts/output.schema.ts, .planning/phases/04-structured-ai-risk-reporting/04-CONTEXT.md</read_first>
  <action>Create a strict Zod schema for risk reports with required sections: overall risk + confidence, critical zones with phase context, actionable staffing recommendations, assumptions/limitations, machine-readable evidence, and executive summary. Reject extra/missing keys by default. Export type-safe helpers from `src/reporting/index.ts` for both AI parse and fallback generation flows.</action>
  <verify>npm run test -- tests/ui/reporting/riskReportSchema.test.ts</verify>
  <acceptance_criteria>
    - `riskReport.schema.ts` contains all mandatory section keys as required fields.
    - Test proves missing or extra fields fail parse.
    - Test proves schema-valid object parses without coercion side effects.
  </acceptance_criteria>
  <done>Strict schema contract is the single gate for all report content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic fallback report generator</name>
  <files>src/reporting/fallback/buildDeterministicRiskReport.ts, src/reporting/fallback/rules.ts, tests/ui/reporting/fallbackReport.test.ts</files>
  <read_first>src/simulation/contracts/output.schema.ts, src/simulation/core/severity.ts</read_first>
  <action>Implement deterministic fallback rules driven by `SimulationOutput` primitives only. Produce full schema-complete report content with no AI dependency and parse output through `RiskReportSchema` before return. Keep rule helpers in `rules.ts` so logic remains testable and auditable.</action>
  <verify>npm run test -- tests/ui/reporting/fallbackReport.test.ts</verify>
  <acceptance_criteria>
    - Fallback output remains stable for identical simulation input.
    - Fallback includes all mandatory sections and non-empty actionable recommendations.
    - Fallback builder never reads network state or mutable UI state.
  </acceptance_criteria>
  <done>Fallback path is deterministic, full-structured, and contract-valid.</done>
</task>

</tasks>

<verification>
- `npm run test -- tests/ui/reporting/riskReportSchema.test.ts tests/ui/reporting/fallbackReport.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Strict report contract exists and rejects malformed payloads.
- Deterministic fallback generator produces full report output for any valid simulation result.
- Contract and fallback behavior are enforced with focused tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-structured-ai-risk-reporting/04-01-SUMMARY.md`
</output>
