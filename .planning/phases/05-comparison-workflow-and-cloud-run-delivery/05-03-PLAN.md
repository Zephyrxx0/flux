---
phase: 05-comparison-workflow-and-cloud-run-delivery
plan: 03
type: execute
wave: 2
depends_on: [05-01-PLAN.md, 05-02-PLAN.md]
files_modified:
  - Dockerfile
  - nginx.conf
  - .dockerignore
  - docs/deployment/cloud-run.md
  - package.json
  - tests/ui/deployment/cloudRunSmoke.test.ts
  - .planning/STATE.md
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
must_haves:
  truths:
    - "App ships as a containerized static build served by Nginx on Cloud Run."
    - "Deployment instructions are reproducible and aligned with Cloud Run runtime expectations."
    - "Demo acceptance verifies run.app availability, SPA fallback routing, and run->compare->export workflow readiness."
  artifacts:
    - path: "Dockerfile"
      provides: "multi-stage container build for Vite static assets + nginx runtime"
    - path: "nginx.conf"
      provides: "SPA fallback and static asset serving behavior"
    - path: "docs/deployment/cloud-run.md"
      provides: "manual deploy and verification playbook"
  key_links:
    - from: "Dockerfile"
      to: "nginx.conf"
      via: "runtime container serves compiled dist with SPA fallback"
      pattern: "try_files|/usr/share/nginx/html"
    - from: "docs/deployment/cloud-run.md"
      to: "tests/ui/deployment/cloudRunSmoke.test.ts"
      via: "documented acceptance criteria reflected in smoke checks"
      pattern: "run.app|SPA refresh|compare export"
---

<objective>
Package and validate Cloud Run delivery for the full Phase 5 demo workflow.

Purpose: Finalize DEP-01 and DEP-02 deployment requirements while proving end-to-end demo viability for comparison and export features.
Output: container artifacts, deployment runbook, and smoke verification for demo acceptance path.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-comparison-workflow-and-cloud-run-delivery/05-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Cloud Run container artifacts for static SPA delivery</name>
  <files>Dockerfile, nginx.conf, .dockerignore</files>
  <read_first>.planning/research/STACK.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>Create a multi-stage Dockerfile (build with Node, serve with Nginx), add SPA fallback routing in nginx config, and define docker ignore rules to keep builds reproducible.</action>
  <verify>docker build -t prompt-wars:phase5 .</verify>
  <acceptance_criteria>
    - Container builds successfully from repo root.
    - Nginx serves static assets and routes unknown paths to index.html.
  </acceptance_criteria>
  <done>DEP-02 packaging baseline is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document manual Cloud Run deployment and verification workflow</name>
  <files>docs/deployment/cloud-run.md, package.json</files>
  <read_first>.planning/phases/05-comparison-workflow-and-cloud-run-delivery/05-CONTEXT.md, .vscode/mcp.json</read_first>
  <action>Document repeatable `gcloud` deployment steps, required env setup, and acceptance verification steps. Add convenient npm scripts for build and deployment command scaffolding where appropriate.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Runbook includes required prerequisites and copy-paste-ready deploy commands.
    - Acceptance checklist explicitly covers run.app availability, SPA refresh, and run->compare->export flow.
  </acceptance_criteria>
  <done>DEP-01 delivery flow is documented and reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add demo smoke checks for deployment acceptance path</name>
  <files>tests/ui/deployment/cloudRunSmoke.test.ts, .planning/STATE.md</files>
  <read_first>docs/deployment/cloud-run.md, src/visualization/components/VisualizationWorkspace.tsx</read_first>
  <action>Add smoke-level checks for expected deployment assumptions and route behavior contracts; update state notes with deployment readiness checkpoint for phase execution tracking.</action>
  <verify>npm run test -- tests/ui/deployment/cloudRunSmoke.test.ts</verify>
  <acceptance_criteria>
    - Smoke checks guard key demo path assumptions.
    - State file records readiness checkpoint for public demo verification.
  </acceptance_criteria>
  <done>Phase 5 deployment acceptance is test-backed and traceable.</done>
</task>

</tasks>
