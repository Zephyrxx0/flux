---
phase: 05-comparison-workflow-and-cloud-run-delivery
plan: 02
type: execute
wave: 1
depends_on: [05-01-PLAN.md]
files_modified:
  - src/comparison/narrative/buildSensitivityNarrative.ts
  - src/comparison/components/SensitivityNarrative.tsx
  - src/export/contracts/briefingExport.schema.ts
  - src/export/buildBriefingExport.ts
  - src/export/renderBriefingHtml.ts
  - src/export/components/ExportActions.tsx
  - src/visualization/components/VisualizationWorkspace.tsx
  - tests/ui/comparison/sensitivityNarrative.test.ts
  - tests/ui/export/briefingExport.test.ts
autonomous: true
requirements: [CMP-02, DEP-03]
must_haves:
  truths:
    - "Sensitivity narrative quantifies intervention impact with absolute delta, percent delta, and severity transition."
    - "Narrative focuses on top three changed zones plus overall risk summary."
    - "Export artifacts include JSON and print-friendly HTML with required briefing sections."
  artifacts:
    - path: "src/comparison/narrative/buildSensitivityNarrative.ts"
      provides: "deterministic narrative builder for intervention sensitivity"
      exports: ["buildSensitivityNarrative"]
    - path: "src/export/buildBriefingExport.ts"
      provides: "JSON export payload builder for briefing artifact"
      exports: ["buildBriefingExport"]
    - path: "src/export/renderBriefingHtml.ts"
      provides: "print-friendly HTML rendering for briefing output"
      exports: ["renderBriefingHtml"]
  key_links:
    - from: "src/comparison/selectors/buildComparisonViewModel.ts"
      to: "src/comparison/narrative/buildSensitivityNarrative.ts"
      via: "top-zone delta model feeds narrative output"
      pattern: "absoluteDelta|percentDelta|severityTransition"
    - from: "src/export/buildBriefingExport.ts"
      to: "src/export/renderBriefingHtml.ts"
      via: "single export contract rendered in machine + human formats"
      pattern: "metadata|topChanges|recommendations|assumptions"
---

<objective>
Deliver decision-grade intervention narratives and export artifacts for Phase 5.

Purpose: Convert comparison deltas into operations-ready narrative guidance and provide briefing artifacts suitable for machine and human consumption.
Output: sensitivity narrative module, export contract/builders, print HTML renderer, and workspace export actions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-comparison-workflow-and-cloud-run-delivery/05-CONTEXT.md
@.planning/phases/04-structured-ai-risk-reporting/04-CONTEXT.md
@src/reporting/contracts/riskReport.schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic sensitivity narrative builder</name>
  <files>src/comparison/narrative/buildSensitivityNarrative.ts, src/comparison/components/SensitivityNarrative.tsx, tests/ui/comparison/sensitivityNarrative.test.ts</files>
  <read_first>src/comparison/selectors/buildComparisonViewModel.ts, .planning/phases/05-comparison-workflow-and-cloud-run-delivery/05-CONTEXT.md</read_first>
  <action>Generate narrative blocks that include per-zone absolute delta, percent delta, severity transition, and an overall risk summary. Limit displayed zones to top three by meaningful risk change.</action>
  <verify>npm run test -- tests/ui/comparison/sensitivityNarrative.test.ts</verify>
  <acceptance_criteria>
    - Narrative lines include absolute and percent values.
    - Severity transition is explicit (for example, red to amber).
    - Top-three zone rule is enforced deterministically.
  </acceptance_criteria>
  <done>Narrative output is concise, quantified, and briefing-ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build briefing export contracts and JSON output</name>
  <files>src/export/contracts/briefingExport.schema.ts, src/export/buildBriefingExport.ts, tests/ui/export/briefingExport.test.ts</files>
  <read_first>src/reporting/contracts/riskReport.schema.ts, src/comparison/selectors/buildComparisonViewModel.ts</read_first>
  <action>Create strict export schema and builder for required content: compared run metadata, top-three zone transitions, action recommendation summary, and assumptions/limitations.</action>
  <verify>npm run test -- tests/ui/export/briefingExport.test.ts</verify>
  <acceptance_criteria>
    - JSON export validates against schema.
    - Export includes all required sections from context decisions.
  </acceptance_criteria>
  <done>JSON briefing artifact is stable and complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render print-friendly HTML briefing and wire export actions</name>
  <files>src/export/renderBriefingHtml.ts, src/export/components/ExportActions.tsx, src/visualization/components/VisualizationWorkspace.tsx, tests/ui/export/briefingExport.test.ts</files>
  <read_first>src/visualization/components/VisualizationWorkspace.tsx, src/export/buildBriefingExport.ts</read_first>
  <action>Render a print-friendly HTML briefing view from the same export contract and wire workspace-level export actions for downloading JSON and opening/printing HTML summary.</action>
  <verify>npm run test -- tests/ui/export/briefingExport.test.ts</verify>
  <acceptance_criteria>
    - Export actions are reachable from workspace UI.
    - HTML output preserves briefing content hierarchy and prints cleanly.
  </acceptance_criteria>
  <done>DEP-03 export workflow is end-user ready.</done>
</task>

</tasks>
