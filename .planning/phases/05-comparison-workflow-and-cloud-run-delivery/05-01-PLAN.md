---
phase: 05-comparison-workflow-and-cloud-run-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useScenarioStore.ts
  - src/hooks/useComparisonStore.ts
  - src/comparison/contracts/comparison.schema.ts
  - src/comparison/selectors/buildComparisonViewModel.ts
  - src/comparison/components/ComparisonPanel.tsx
  - src/visualization/components/VisualizationWorkspace.tsx
  - tests/ui/comparison/comparisonStore.test.ts
  - tests/ui/comparison/buildComparisonViewModel.test.ts
autonomous: true
requirements: [CMP-01, CMP-03]
must_haves:
  truths:
    - "Users can select explicit baseline and candidate runs from persisted run history."
    - "Persisted run history remains schema-validated and compatible with existing store persistence behavior."
    - "Comparison view model is deterministic and suitable for downstream narrative/export generation."
  artifacts:
    - path: "src/comparison/contracts/comparison.schema.ts"
      provides: "schema contract for persisted run records and comparison pair selection"
      exports: ["PersistedRunSchema", "ComparisonPairSchema"]
    - path: "src/hooks/useComparisonStore.ts"
      provides: "comparison state and baseline/candidate selection lifecycle"
      exports: ["useComparisonStore"]
    - path: "src/comparison/selectors/buildComparisonViewModel.ts"
      provides: "deterministic delta model for top-zone comparison summaries"
      exports: ["buildComparisonViewModel"]
  key_links:
    - from: "src/hooks/useScenarioStore.ts"
      to: "src/hooks/useComparisonStore.ts"
      via: "run completion appends persisted run snapshot"
      pattern: "setLatestSimulationOutput|persist"
    - from: "src/comparison/selectors/buildComparisonViewModel.ts"
      to: "src/visualization/components/VisualizationWorkspace.tsx"
      via: "comparison panel receives normalized baseline/candidate deltas"
      pattern: "latestSimulationOutput|ComparisonPanel"
---

<objective>
Implement persisted run-history comparison foundations for Phase 5.

Purpose: Enable explicit baseline/candidate run comparison from validated persisted history, with deterministic delta outputs for sensitivity and export flows.
Output: comparison schemas, comparison store, deterministic view-model selector, and initial panel integration.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-comparison-workflow-and-cloud-run-delivery/05-CONTEXT.md
@src/hooks/useScenarioStore.ts
@src/simulation/contracts/output.schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define persisted run and comparison contracts</name>
  <files>src/comparison/contracts/comparison.schema.ts, tests/ui/comparison/buildComparisonViewModel.test.ts</files>
  <read_first>src/simulation/contracts/output.schema.ts, src/hooks/useScenarioStore.ts</read_first>
  <action>Create strict schemas for persisted run entries (id, timestamp, scenario label, run hash, simulation output snapshot metadata) and baseline/candidate pair selection. Ensure schema validates historical records before hydration.</action>
  <verify>npm run test -- tests/ui/comparison/buildComparisonViewModel.test.ts</verify>
  <acceptance_criteria>
    - Persisted run records reject malformed payloads.
    - Comparison pair schema enforces explicit baseline and candidate ids.
  </acceptance_criteria>
  <done>Comparison contracts are stable for storage and UI selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement comparison store with explicit pair selection</name>
  <files>src/hooks/useComparisonStore.ts, src/hooks/useScenarioStore.ts, tests/ui/comparison/comparisonStore.test.ts</files>
  <read_first>src/hooks/useScenarioStore.ts, .planning/phases/05-comparison-workflow-and-cloud-run-delivery/05-CONTEXT.md</read_first>
  <action>Add a comparison store that records completed runs to persisted history, allows selecting baseline/candidate runs explicitly, and exposes current comparison pair state for UI consumption.</action>
  <verify>npm run test -- tests/ui/comparison/comparisonStore.test.ts</verify>
  <acceptance_criteria>
    - New simulation runs append to persisted run history.
    - User can select explicit baseline and candidate runs.
    - History remains available across refresh after schema validation.
  </acceptance_criteria>
  <done>Run history and explicit pairing flow are operational.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build deterministic comparison view model and mount panel shell</name>
  <files>src/comparison/selectors/buildComparisonViewModel.ts, src/comparison/components/ComparisonPanel.tsx, src/visualization/components/VisualizationWorkspace.tsx, tests/ui/comparison/buildComparisonViewModel.test.ts</files>
  <read_first>src/visualization/components/VisualizationWorkspace.tsx, src/reporting/contracts/riskReport.schema.ts</read_first>
  <action>Compute deterministic absolute and percent deltas for zone-level metrics, severity transitions, and overall risk summary. Render a comparison panel shell in the workspace that reads explicit baseline/candidate selection state.</action>
  <verify>npm run test -- tests/ui/comparison/buildComparisonViewModel.test.ts</verify>
  <acceptance_criteria>
    - Delta model outputs absolute + percent + severity transition fields.
    - Comparison panel mounts without breaking existing visualization/report panels.
  </acceptance_criteria>
  <done>Comparison foundations are ready for narrative and export work.</done>
</task>

</tasks>
