---
phase: 02-scenario-configuration-experience
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/components/config/ScenarioForm.tsx, src/components/config/ValidationList.tsx, src/components/config/ScenarioSidebar.tsx, src/simulation/index.ts, tests/ui/form.test.ts, tests/ui/calibration.test.ts, tests/ui/run.test.ts, package.json]
autonomous: true
requirements: [CFG-01, CFG-03]
user_setup: []

must_haves:
  truths:
    - "User can edit zones, gates, phases, and arrivals in a unified form"
    - "Advanced calibration settings are hidden behind collapsible accordions"
    - "Clicking 'Run' validates the form and shows an error list if invalid"
    - "A valid 'Run' triggers the deterministic simulation engine"
  artifacts:
    - path: "src/components/config/ScenarioForm.tsx"
      provides: "Zod-driven configuration form"
    - path: "src/components/config/ValidationList.tsx"
      provides: "On-run error display"
  key_links:
    - from: "src/components/config/ScenarioForm.tsx"
      to: "src/simulation/core/simulateDeterministic.ts"
      via: "StadiumSim.run call"
    - from: "src/components/config/ScenarioForm.tsx"
      to: "src/hooks/useScenarioStore.ts"
      via: "form.reset(currentInput) in useEffect"
---

<objective>
Build the Scenario Configuration Panel with a complex Zod-driven form, collapsible advanced sections, and an explicit "Run" trigger with error reporting.

Purpose: Deliver the core interactive configuration experience (CFG-01, CFG-03, D-03, D-04, D-05).
Output: A fully functional configuration form connected to the simulation engine and the scenario store.
</objective>

<execution_context>
@D:/Codes/Personal/prompt-wars/.gemini/get-shit-done/workflows/execute-plan.md
@D:/Codes/Personal/prompt-wars/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scenario-configuration-experience/02-02-SUMMARY.md
@src/simulation/contracts/input.schema.ts
@src/simulation/adapters/StadiumSim.ts
@src/hooks/useScenarioStore.ts
</context>

<interfaces>
From src/simulation/adapters/StadiumSim.ts:
```typescript
export class StadiumSim {
  static run(input: SimulationInput): SimulationOutput;
}
```

From src/hooks/useScenarioStore.ts (created in 02-02):
```typescript
export const useScenarioStore = create<ScenarioState>()((set) => ({
  currentInput: initialInput,
  // ...
}));
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Scenario Form with Nested Arrays and Store Wiring</name>
  <files>src/components/config/ScenarioForm.tsx, src/components/config/ScenarioSidebar.tsx, tests/ui/form.test.ts</files>
  <action>
    1. Create `ScenarioForm` using `react-hook-form` and `zodResolver(SimulationInputSchema)`.
    2. Use `useFieldArray` for `zones`, `gates`, `phases`, and `arrivals`.
    3. Implement form fields for basic stadium parameters (capacity, mode) and list editors.
    4. **Wiring:** Subscribe to `useScenarioStore`'s `currentInput`.
    5. **Preset Sync:** Add a `useEffect` that calls `form.reset(currentInput)` whenever `currentInput` changes from the store. This ensures presets applied via the toolbar update the form fields correctly.
    6. Wrap array items in `React.memo` for performance (per RESEARCH.md).
    7. Integrate `ScenarioForm` into `ScenarioSidebar`.
    8. Create `tests/ui/form.test.ts` to verify ScenarioForm handles nested arrays and wires to store.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/form.test.ts && graphify update .</automated>
  </verify>
  <done>Configuration form handles nested simulation input arrays and syncs with the scenario store.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Advanced Calibration Accordions</name>
  <files>src/components/config/ScenarioForm.tsx, tests/ui/calibration.test.ts</files>
  <action>
    1. Add `accordion` component via `npx shadcn@latest add accordion`.
    2. Wrap advanced controls (e.g., Detailed Mode sub-zone settings) in collapsible `Accordion` sections (D-05, CFG-03).
    3. Ensure these sections are placed logically within the form categories (e.g., Gate throughput calibration under Gates).
    4. Create `tests/ui/calibration.test.ts` to verify advanced calibration sections are collapsible.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/calibration.test.ts && graphify update .</automated>
  </verify>
  <done>Advanced calibration controls are accessible but non-distracting.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Run Trigger and Error List</name>
  <files>src/components/config/ValidationList.tsx, src/components/config/ScenarioForm.tsx, tests/ui/run.test.ts, package.json</files>
  <action>
    1. Add `alert` component via `npx shadcn@latest add alert`.
    2. Create `ValidationList.tsx` to display flattened Zod errors (D-04).
    3. Implement the "Run" button in `ScenarioForm.tsx` (D-03).
    4. On "Run" click:
       - Validate using Zod.
       - If invalid: update `ValidationList` state with errors.
       - If valid: clear errors, update the store with latest form values, and call `StadiumSim.run(input)`.
    5. For now, log the simulation result to the console (Visualization is Phase 3).
    6. Create `tests/ui/run.test.ts` to verify the Run button validates and triggers the engine.
    7. Update `test:smoke` script in `package.json` to: `npx vitest run tests/ui/form.test.ts tests/simulation/presets.test.ts --bail 1`.
  </action>
  <verify>
    <automated>npm run test:smoke && npx vitest run tests/ui/run.test.ts && graphify update .</automated>
  </verify>
  <done>Users receive clear feedback on invalid inputs and can trigger simulations, and the smoke test script is updated to the full UI suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form Input → Simulation Engine | User provided data enters the core logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | ValidationList | accept | Validation errors are local and describe the user's own input; no sensitive data risk. |
| T-02-04 | Spoofing | SimulationInput | mitigate | Strictly enforce SimulationInputSchema on the form submission path. |
</threat_model>

<verification>
Check if clicking "Run" with invalid data shows the error list, and valid data triggers the engine. Verify that applying a preset updates the form fields.
</verification>

<success_criteria>
- Unified configuration panel is fully functional.
- Nested data (Gates, Arrivals, etc.) can be edited.
- Advanced sections are collapsible.
- "Run" button provides validation feedback and triggers simulation.
- Presets update form state via store subscription.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scenario-configuration-experience/02-03-SUMMARY.md`
</output>
