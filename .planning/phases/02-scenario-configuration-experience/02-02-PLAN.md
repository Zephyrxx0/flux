---
phase: 02-scenario-configuration-experience
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/hooks/useScenarioStore.ts, src/simulation/presets.ts, src/components/config/PresetsToolbar.tsx, tests/simulation/presets.test.ts, tests/ui/store.test.ts, tests/ui/presets.test.ts]
autonomous: true
requirements: [CFG-02]
user_setup: []

must_haves:
  truths:
    - "Scenario state is persisted to LocalStorage"
    - "Presets (Normal, Rush, Crisis) can be applied to the state"
    - "Users can see a toolbar with preset buttons"
  artifacts:
    - path: "src/hooks/useScenarioStore.ts"
      provides: "Zustand store with persistence"
    - path: "src/simulation/presets.ts"
      provides: "Pre-defined simulation inputs"
  key_links:
    - from: "src/components/config/PresetsToolbar.tsx"
      to: "src/hooks/useScenarioStore.ts"
      via: "applyPreset action"
---

<objective>
Implement state management for scenarios, including persistence in LocalStorage and the logic for applying functional presets.

Purpose: Enable "Saved Scenarios" and "One-click Presets" (D-02, D-06, CFG-02).
Output: A persisted Zustand store and a Preset Toolbar component.
</objective>

<execution_context>
@D:/Codes/Personal/prompt-wars/.gemini/get-shit-done/workflows/execute-plan.md
@D:/Codes/Personal/prompt-wars/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scenario-configuration-experience/02-01-SUMMARY.md
@src/simulation/contracts/input.schema.ts
</context>

<interfaces>
From src/simulation/contracts/input.schema.ts:
```typescript
export interface SimulationInput {
  schemaVersion: number;
  mode: "zone" | "detailed";
  phases: Phase[];
  zones: Zone[];
  gates: Gate[];
  arrivals: Arrival[];
  detailed?: DetailedMode;
}
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Implement Presets Logic</name>
  <files>src/simulation/presets.ts, tests/simulation/presets.test.ts</files>
  <behavior>
    - 'Normal Event' preset: Realistic baseline with moderate demand.
    - 'Sold Out Rush' preset: High demand, compressed arrivals.
    - 'Gate Crisis' preset: Reduced gate throughput in specific zones.
  </behavior>
  <action>
    1. Define `presets` as a record of `SimulationInput` objects in `src/simulation/presets.ts`.
    2. Create `tests/simulation/presets.test.ts` and write tests to ensure each preset is valid according to `SimulationInputSchema`.
  </action>
  <verify>
    <automated>npx vitest run tests/simulation/presets.test.ts && graphify update .</automated>
  </verify>
  <done>Functional presets are defined and validated against the schema.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Zustand Store with Persistence</name>
  <files>src/hooks/useScenarioStore.ts, tests/ui/store.test.ts</files>
  <action>
    1. Create a Zustand store `useScenarioStore` using `persist` middleware.
    2. State includes: `currentInput` (SimulationInput) and `savedScenarios` (Record of SimulationInput).
    3. Actions: `updateInput`, `saveScenario`, `loadScenario`, `applyPreset`.
    4. Implement Zod validation when loading from LocalStorage to prevent state injection (per Threat Model).
    5. Create `tests/ui/store.test.ts` to verify the Zustand store persists and validates LocalStorage.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/store.test.ts && graphify update .</automated>
  </verify>
  <done>Scenario state is managed by Zustand and persists to LocalStorage.</done>
</task>

<task type="auto">
  <name>Task 3: Create Presets Toolbar Component</name>
  <files>src/components/config/PresetsToolbar.tsx, src/components/layout/AppLayout.tsx, tests/ui/presets.test.ts</files>
  <action>
    1. Build `PresetsToolbar` using shadcn/ui buttons (D-02).
    2. Buttons for: "Normal", "Rush", "Crisis".
    3. Clicking a button calls `applyPreset` in `useScenarioStore`.
    4. Integrate the toolbar into the top of the Sidebar in `AppLayout.tsx`.
    5. Create `tests/ui/presets.test.ts` to verify PresetsToolbar triggers store actions.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/presets.test.ts && graphify update .</automated>
  </verify>
  <done>Preset toolbar is visible and wired to the scenario store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LocalStorage → App State | Data loaded from disk could be tampered with. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | useScenarioStore | mitigate | Validate data loaded from LocalStorage using SimulationInputSchema before hydrating store. |
</threat_model>

<verification>
Verify presets can be applied and state persists across page refresh (manually or via test).
</verification>

<success_criteria>
- Zustand store manages scenario state and persistence.
- Presets are defined and valid.
- Preset toolbar is integrated into the layout.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scenario-configuration-experience/02-02-SUMMARY.md`
</output>
