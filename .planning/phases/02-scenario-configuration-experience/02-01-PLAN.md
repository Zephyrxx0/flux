---
phase: 02-scenario-configuration-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vitest.config.ts, src/main.tsx, src/App.tsx, src/components/layout/AppLayout.tsx, src/index.css]
autonomous: true
requirements: [CFG-01]

must_haves:
  truths:
    - "React application boots with Vite and Tailwind CSS v4"
    - "Persistent Sidebar layout is visible on load"
    - "Vitest environment is configured for browser-like testing (jsdom)"
  artifacts:
    - path: "package.json"
      provides: "React/Vite/Tailwind/shadcn dependencies"
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Persistent Sidebar shell"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React Root render"
---

<objective>
Initialize the UI foundation for the Scenario Configuration Experience using React, Vite, Tailwind CSS v4, and shadcn/ui.

Purpose: Provide the container and layout for the configuration panel (D-01, D-07, D-08).
Output: A working React application with a persistent Sidebar layout.
</objective>

<execution_context>
@D:/Codes/Personal/prompt-wars/.gemini/get-shit-done/workflows/execute-plan.md
@D:/Codes/Personal/prompt-wars/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scenario-configuration-experience/02-RESEARCH.md
@.planning/phases/02-scenario-configuration-experience/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap React + Vite + Tailwind 4 + shadcn/ui</name>
  <files>package.json, vitest.config.ts, src/index.css, src/main.tsx, src/App.tsx, components.json</files>
  <action>
    1. Update `package.json` with React 19, Vite 8, Tailwind 4, and shadcn dependencies per RESEARCH.md.
    2. Add `"test:smoke": "npx vitest run tests/simulation/determinism.test.ts"` to `package.json` scripts.
    3. Update `vitest.config.ts` to use `jsdom` environment and `@vitejs/plugin-react`.
    4. Initialize Tailwind CSS v4 in `src/index.css` using `@import "tailwindcss";`.
    5. Setup `src/main.tsx` and a basic `src/App.tsx`.
    6. Run `npx shadcn@latest init -d` (default/non-interactive) to initialize shadcn/ui.
    7. Verify the dev server starts and Vitest passes a smoke test.
  </action>
  <verify>
    <automated>npm run test:smoke && npm list react vite tailwindcss && graphify update .</automated>
  </verify>
  <done>React + Tailwind 4 environment is ready and tests run in jsdom.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Persistent Sidebar Layout</name>
  <files>src/components/layout/AppLayout.tsx, src/components/ui/sidebar.tsx, src/App.tsx, tests/ui/layout.test.ts</files>
  <action>
    1. Add `sidebar` component via `npx shadcn@latest add sidebar`.
    2. Create `src/components/layout/AppLayout.tsx` implementing the Persistent Sidebar pattern (D-01).
    3. The layout should have a Sidebar on the left for configuration and a main content area for future visualization.
    4. Update `src/App.tsx` to use the `AppLayout`.
    5. Create `tests/ui/layout.test.ts` to verify AppLayout renders the Sidebar component.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/layout.test.ts && graphify update .</automated>
  </verify>
  <done>Application has a persistent left sidebar layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Runtime | Initial environment setup, no external input yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | package.json | mitigate | Use exact versions in package.json to avoid supply chain drift. |
</threat_model>

<verification>
Check if `npm run dev` (or equivalent) starts and the sidebar is rendered in a browser/screenshot.
</verification>

<success_criteria>
- Vite/React/Tailwind 4 app is functional.
- Persistent Sidebar layout is implemented.
- Vitest is configured for UI testing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scenario-configuration-experience/02-01-SUMMARY.md`
</output>
